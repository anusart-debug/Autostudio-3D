# CHANGELOG

เก็บเฉพาะการตัดสินใจที่เปลี่ยนสถาปัตยกรรม ไม่ใช่ทุกการแก้

## v41 (กำลังทำ) — แก้ความเข้าใจผิด: "คงฉากหลังเดิมทั้งหมด" ควรคงแค่โลเคชั่น ไม่ใช่ทุกอย่าง
คอมมิตก่อนหน้าปิดใช้งานมุมกล้อง/เลนส์/แสง/สภาพอากาศ/สไตล์ทั้งหมดตอนติ๊ก `#adKeepScene` — ผู้ใช้แจ้ง
กลับว่านั่นไม่ใช่ที่ต้องการ: "คงฉากหลังไว้อย่างเดียว ส่วนอื่นๆให้ปรับแก้ไขได้ เพื่อภาพจะได้ออกมาสวยงาม
อย่างมืออาชีพ" — เจตนาจริงคือคงแค่ **โลเคชั่น/สถานที่จริงในภาพ** ไว้เท่านั้น ส่วนมุมกล้อง เลนส์ แสง เวลา
และสไตล์การเรนเดอร์ต้องยังปรับได้และมีผลจริง (เหมือนช่างภาพมืออาชีพรีทัชภาพเดิมให้สวยขึ้น ไม่ใช่ก็อปภาพ
เดิมมาเฉยๆ) — สอดคล้องกับตรรกะเดียวกันที่ใช้กับ `#lockLoc` ในโหมดแก้ภาพ (edit mode) ที่ทำมาก่อนแล้ว
- `buildAdPrompt()`/`buildAdPromptTH()` (`36-edit-prompt.js`) — สาขา `keepScene` เปลี่ยนจาก "ห้ามเปลี่ยน
  ฉากหลัง มุมกล้อง เวลา หรือแสง" (ล็อกทุกอย่าง) เป็น "คงสถานที่จริงในภาพไว้ ไม่ย้าย" + ยังใส่มุมกล้อง/เลนส์/
  แสง/สภาพอากาศ/HDR ตามที่เลือกไว้ พร้อมสั่งแต่งแสง-เงา-เกรดสีให้สวยระดับมืออาชีพ ให้เข้ากับมุมกล้องและ
  แสงใหม่โดยยังอยู่ที่เดิม — เพิ่มสไตล์การเรนเดอร์ (`list("style")`) และ HDR (`S.hdr`) เข้าไปในทั้งสองสาขา
  (`keepScene` ติ๊กหรือไม่ก็ตาม) เพราะเดิมไม่ถูกใช้เลยไม่ว่ากรณีใด (ช่องว่างเดิมของโหมดนี้ ไม่เกี่ยวกับ
  `adKeepScene`)
- `updateAdLocks()` (`30-prompt.js`) — ตัดการปิดใช้งาน `#angleChips`/`#lens`/`#lightChips`/`#weather`/
  `#styleChips`/`#hdr` ออกทั้งหมด เหลือปิดเฉพาะ `#locChips` เมื่อ `adReady()&&adKeepScene` เท่านั้น
  (ลบ `angleAdNote`/`lightAdNote`/`styleAdNote` ที่ไม่ได้ใช้แล้วออกจาก `index.html`)
- เช็กลิสต์ตรวจสอบ (`40-review.js`) — รวม `keepLocActive` ให้ครอบคลุมทั้งสองโหมด (`lockLoc` ในโหมดแก้ภาพ
  และ `adKeepScene` ในโหมดเปลี่ยนโฆษณา) ใช้ข้อความ "คงจากภาพต้นแบบ (ไม่เปลี่ยน)" เดียวกัน · เปลี่ยนป้าย
  "คงฉากเดิม" เป็น "คงโลเคชั่นเดิม" ให้ตรงความหมายใหม่
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์ · ทดสอบจริงใน
  เบราว์เซอร์ — ติ๊ก `adKeepScene` แล้วเลือกมุมกล้อง/แสง/สไตล์ที่ไม่ใช่ค่าเริ่มต้น เห็นคำสั่งจริง (อังกฤษและ
  ไทย) ใส่ค่าที่เลือกไว้ครบทุกตัว พร้อมกับยังคงประโยค "อยู่ที่เดิม ไม่ย้ายสถานที่"

## v41 (กำลังทำ) — ปิดใช้งานช่องที่ไม่มีผลจริงในโหมดเปลี่ยนโฆษณา (05/06/08)
ผู้ใช้รายงาน: ติ๊ก "คงฉากหลังเดิมทั้งหมด" (`#adKeepScene`) แล้วฉากหลังไม่เปลี่ยนตามที่ตั้งใจ (ถูกแล้ว)
แต่ยังกดเลือกมุมกล้อง/แสง/เวลา/สไตล์ได้อยู่ ทั้งที่ค่าพวกนั้นไม่มีผลกับคำสั่งจริง — ตรวจ `buildAdPrompt()`/
`buildAdPromptTH()` (`36-edit-prompt.js`) แล้วพบว่า:
- เมื่อ `adKeepScene` ติ๊กอยู่: โลเคชั่น มุมกล้อง เลนส์ แสง สภาพอากาศ **ไม่ถูกอ่านเลย** (สาขา `if(keepScene)`
  เขียนแค่ "อย่าเปลี่ยนพื้นหลัง มุมกล้อง เวลา หรือแสง")
- **ไม่ว่าจะติ๊กหรือไม่ก็ตาม**: สไตล์การเรนเดอร์ (`list("style")`) และ HDR/Contrast (`S.hdr`) ไม่ถูกใช้ใน
  `buildAdPrompt()` เลยแม้แต่บรรทัดเดียว — เป็นช่องว่างเดิมของโหมดเปลี่ยนโฆษณา ไม่เกี่ยวกับ `adKeepScene`

แก้โดยทำให้ UI ซื่อสัตย์กับคำสั่งจริง (ตามธรรมเนียมโปรเจกต์) แทนที่จะปล่อยให้เลือกอะไรก็ได้ที่เงียบๆไม่มีผล:
- เพิ่ม `updateAdLocks()`/`setChipsLock()` ใน `30-prompt.js` เรียกทุกครั้งที่ `sync()` (คือทุกครั้งที่มีการ
  เปลี่ยนแปลงในแอป) — ปิด `#locChips`/`#angleChips`+`#lens`/`#lightChips`+`#weather` (ทึบ+กดไม่ได้ผ่าน
  CSS class `.chipsOff` และ `disabled`) เฉพาะตอน `adReady()&&adKeepScene` และปิด `#styleChips`+`#hdr`
  เสมอเมื่อ `adReady()` พร้อมโชว์กล่องเตือน (`.dnaline.warn`) อธิบายว่าทำไมถึงไม่มีผล
- ซ่อนเช็กบ็อกซ์ `#lockLocField` (ฟีเจอร์ "ไม่กำหนดโลเคชั่น" ที่เพิ่มไปก่อนหน้า) เมื่อ `adReady()` ด้วย —
  ในโหมดเปลี่ยนโฆษณา `buildAdPrompt()` ไม่อ่านค่านี้เลย มีสวิตช์ `adKeepScene` ของตัวเองอยู่แล้ว โชว์ทั้ง
  สองพร้อมกันจะสับสนว่าอันไหนคุมอะไร
- เพิ่ม CSS `.chipsOff`, `.dnaline.warn`, `select:disabled`/`input[type=range]:disabled` (`styles.css`)
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์ · ทดสอบจริงใน
  เบราว์เซอร์ — อัปโหลดภาพสื่อ+อาร์ตเวิร์กครบ 2 ใบ, ติ๊ก/ปลดติ๊ก `adKeepScene` แล้วเห็นช่องที่เกี่ยวข้อง
  ล็อก/ปลดล็อกตรงกับพฤติกรรมจริงของ `buildAdPrompt()` ทุกกรณี

## v41 (กำลังทำ) — ย้าย "ไม่กำหนดโลเคชั่น" ไปแสดงในหมวด 04 · Location และเปิดให้ใช้ได้ทุกสื่อ
ผู้ใช้แนบภาพวงกลมล้อมกรอบทั้งหมวด 04 (การ์ดเลือกโลเคชั่น) แล้วขอ "เพิ่มตัวเลือกไม่เลือกกำหนด
โลเคชั่น สำหรับผู้ใช้ที่ต้องการโลเคชั่นตามภาพต้นแบบ" — เดิมตัวเลือกนี้ (`#lockLoc`) อยู่ในแผง
`01B · Reference DNA` (คนละหมวด, ซ่อนอยู่) และโชว์เฉพาะสื่อที่อยู่กับที่ (`stat:1`) เท่านั้น ผู้ใช้มองไม่
เห็นตอนอยู่ที่หมวดเลือกโลเคชั่น จึงย้าย:
- ย้ายเช็กบ็อกซ์ `#lockLocField` จาก `#dnaBlock` (01B, ซ้าย) ไปไว้เหนือ `#locChips` ทันที ในหมวด
  `04 · Location` (ขวา) ตรงจุดที่ผู้ใช้วงไว้ (`index.html`)
- เงื่อนไขการโชว์เปลี่ยนจาก "เฉพาะสื่อที่อยู่กับที่" (`subjInfo().stat`) เป็น "มีภาพต้นแบบแล้ว"
  (`hasSketch()`) ใน `sync()` (`30-prompt.js`) — เพราะตอนนี้เป็นตัวเลือกทั่วไปสำหรับใครก็ตามที่มี
  ภาพต้นแบบและอยากคงสถานที่จริงในภาพไว้ ไม่จำกัดแค่ป้าย/จอ LED อีกต่อไป
- ตัด `SB.stat`/`v.stat` ออกจากเงื่อนไขใน `buildEditPrompt()` (`36-edit-prompt.js`),
  `buildEditPromptTH()`/`updateDnaLine()` (`35-dna.js`) และเช็กลิสต์ตรวจสอบ (`40-review.js`) —
  เหลือแค่เช็ก `$("lockLoc").checked` ตัวเดียว ใช้ได้กับทุกประเภทสื่อ
- การ์ดเลือกโลเคชั่นด้านล่างยังทำงานตามปกติ (ยังใช้ดูแผนที่/สายรถเมล์ได้) — ตัวเลือกนี้คุมแค่ประโยค
  โลเคชั่นในคำสั่งที่ส่งให้ AI เท่านั้น ไม่ได้ไปปิดแผนที่หรือ Overpass เพื่อเลี่ยงความเสี่ยงที่จะพัง
  ระบบแผนที่/สายรถเมล์ซึ่งมีเทสต์คุมอยู่แล้วหลายไฟล์
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์ · ทดสอบจริง
  ในเบราว์เซอร์ด้วยสื่อที่เคลื่อนที่ได้ (bus wrap ไม่ใช่ `stat:1`) ยืนยันว่าตัวเลือกโชว์ได้แล้วไม่จำกัด
  ประเภทสื่อ และคำสั่งที่ได้ยังคงสถานที่จากภาพต้นแบบถูกต้อง

## v41 (กำลังทำ) — ตัวเลือก "ไม่เปลี่ยนโลเคชั่น" สำหรับสื่อที่อยู่กับที่ (01B)
ผู้ใช้ขอ: สื่อที่ติดตั้งอยู่กับที่ (ป้ายบิลบอร์ด, จอ LED, bus shelter — `stat:1` ใน `VEHICLES`) เมื่อ
แก้ไขจากภาพต้นแบบ อยากให้คงสถานที่จริงในภาพไว้ ไม่ย้ายไปโลเคชั่นอื่นตามที่เลือกในหมวด 04 แต่ยัง
อยากได้ภาพที่แต่งแสง-เงาสวยแบบมืออาชีพ (ไม่ใช่แค่ก็อปภาพเดิม):
- เพิ่มเช็กบ็อกซ์ `#lockLoc` ในแผง `01B · Reference DNA` (`index.html`) — ซ่อน/โชว์ตาม
  `subjInfo().stat` (คุมที่ `sync()` ใน `30-prompt.js`) เพราะแนวคิดนี้มีผลเฉพาะสื่อที่อยู่กับที่
  รถที่วิ่งได้ (bus wrap, tuk-tuk ฯลฯ) ย้ายโลเคชั่นได้ตามปกติ เช็กบ็อกซ์จึงไม่โชว์ให้สับสน
- `buildEditPrompt()`/`buildEditPromptTH()` (`36-edit-prompt.js`, `35-dna.js`) — เมื่อติ๊กไว้และเป็น
  สื่อที่อยู่กับที่ ประโยคที่เคย "place the billboard on <โลเคชั่นที่เลือก>" เปลี่ยนเป็นสั่งให้คงที่ตั้ง/
  พื้นหลังเดิมจากภาพ ห้ามย้าย แต่ยังคงสั่งแต่งแสง-เงาระดับภาพถ่ายมืออาชีพ (คำแนะนำแสง/สภาพอากาศที่
  เลือกไว้ยังมีผล ใช้เป็นทิศทางรีทัชแทนการย้ายฉาก) — ไม่แตะ `buildAdPrompt()` เพราะโหมดเปลี่ยน
  โฆษณามี `adKeepScene` ที่ทำหน้าที่คล้ายกันอยู่แล้ว (เข้มกว่า คือห้ามเปลี่ยนแสงด้วย) เป็นฟีเจอร์คนละ
  ระดับ ไม่ได้ทับกัน
- เพิ่ม `lockLoc` เข้ารายการ change-listener ที่เรียก `sync()` (`35-dna.js`) — ไม่งั้นติ๊กแล้วคำสั่งจะ
  ไม่อัปเดตจนกว่าจะไปแก้ช่องอื่น (พลาดจุดนี้ตอนแรก เจอจากการทดสอบจริงในเบราว์เซอร์)
- เพิ่ม `lockLoc` เข้า `DV_FIELD_IDS` (`72-project.web.js`) ให้ save/load โปรเจกต์บน Drive คงค่านี้ไว้
  เหมือนเช็กบ็อกซ์ล็อกอื่นๆ
- เช็กลิสต์ตรวจสอบคำสั่ง (`40-review.js`) แก้ให้ "ซื่อสัตย์" ตามธรรมเนียมเดิมของโปรเจกต์ — แถว
  Location จะไม่โชว์ชื่อโลเคชั่นที่เลือกไว้ (เพราะไม่ได้ถูกใช้จริง) แต่โชว์ "คงจากภาพต้นแบบ (ไม่เปลี่ยน)"
  แทน กันผู้ใช้เข้าใจผิดว่าคำสั่งจะสั่งย้ายไปที่นั่น
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์ · ทดสอบจริง
  ในเบราว์เซอร์ (เลือกป้ายบิลบอร์ด + อัปโหลดภาพ + ติ๊ก/ปลดติ๊ก) เห็นคำสั่งเปลี่ยนถูกต้องทั้งอังกฤษ/ไทย
  และเช็กลิสต์ตรวจสอบตรงกับคำสั่งจริง

## v41 (กำลังทำ) — เพิ่มปลายทางที่ 14: AiPASS (TH-AI Passport) ในกล่องส่งงาน (08)
ผู้ใช้ขอเพิ่มเว็บปลายทางใหม่ `https://de.aipass.net/chat` พร้อมโลโก้ — ตรวจสอบก่อนเพิ่มด้วย
`WebFetch` เพราะไม่รู้จักเว็บนี้มาก่อน พบว่า `de.aipass.net` รีไดเรกต์ไปที่ `aipass.go.th`
(โดเมนรัฐบาลไทย `.go.th`) เป็นโครงการ **"TH-AI Passport" (แบรนด์ AiPASS)** ของกระทรวงดิจิทัลเพื่อ
เศรษฐกิจและสังคม + สำนักงานคณะกรรมการดิจิทัลเพื่อเศรษฐกิจและสังคมแห่งชาติ (BDE) — แจกสิทธิ์ใช้ AI
ฟรีกว่า 14 ค่าย (ChatGPT, Claude, Gemini, Perplexity, DeepSeek ฯลฯ) ผ่านแพลตฟอร์มเดียว ต้องยืนยัน
ตัวตนคนไทยผ่านแอปทางรัฐ/ThaID (อายุ 15+ จำกัด 5 ล้านสิทธิ์):
- เพิ่ม `LOGO_AIPASS` (ไฟล์โลโก้จริงที่ผู้ใช้ส่งมา ย่อ 96×96px เหมือนโลโก้อื่น) และรายการใหม่ใน
  `DESTS` (`60-export.js`) — `t:"ฟรี (ยืนยันตัวตน)"` สื่อความจริงว่าฟรีแต่ต้องผ่านการยืนยันตัวตน
  ก่อน ไม่ใช่ "ฟรี" เฉยๆแบบเว็บอื่นที่ไม่มีเงื่อนไข
- ตัวนับ "N ปลายทาง" ในหัวกล่องคำนวณจาก `DESTS.length` อยู่แล้ว ไม่ต้องแก้เพิ่ม (13→14 อัตโนมัติ)
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — ไอคอนมุมกล้อง "hero34"/"low" สลับกัน (05 · Camera)
ผู้ใช้แนบภาพวงกลม+ลูกศรชี้ว่า "3/4 หน้า ฮีโร่ช็อต" กับ "มุมต่ำ อลังการ" ในหมวด 05 ใช้ภาพสลับกันอยู่ —
สลับทั้งภาพ (`ANGLE_ICONS`) และสีพื้นที่สุ่มมาคู่กับภาพนั้นๆ (`ANGLE_TILE_BG`) ให้ตรงกันทั้งคู่
(สีพื้นต้องตามภาพไปด้วยเสมอ ไม่ใช่ค้างกับป้ายชื่อเดิม เพราะ `ANGLE_TILE_BG` สุ่มมาจากมุมของภาพ
แต่ละใบเองตามที่ทำไว้ตอนฝังภาพจริงรอบก่อน) เขียนสคริปต์เล็กๆสลับค่าใน object โดยตรงใน
`src/js/20-chips.js` (ไม่ไล่แก้มือเพราะแต่ละค่าเป็น base64 ยาวมาก):
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — โลโก้ปลายทางรอบสาม วงหมุน 3 เว็บ (Firefly/Microsoft Designer/Dola AI)
รอบก่อนแก้เป็นวงหมุน 4 เว็บรวม Ideogram ด้วย แต่ผู้ใช้แจ้งต่อว่า Ideogram ถูกแล้ว เหลือแค่ Adobe
Firefly/Microsoft Designer/Dola AI ที่ยังวนผิดอยู่ — ภาพลูกศรรอบนี้ชี้เป็นวงหมุน 3 ทอด (Firefly→
Microsoft Designer→Dola AI→กลับมา Firefly) แก้โดยไล่ตามลูกศร:
- Microsoft Designer เปลี่ยนเป็น `LOGO_FIREFLY` (ไล่สีรูปตัว S)
- Adobe Firefly เปลี่ยนเป็น `LOGO_IDEOGRAM` (ป้าย "Fi" สีแดง)
- Dola AI เปลี่ยนเป็น `LOGO_MSFT` (ริบบิ้นม่วง)
- Ideogram/Arena AI/Design Arena ไม่แก้เพิ่ม (ไม่ได้ถูกวงไว้รอบนี้ แปลว่าถูกแล้ว)
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — โลโก้ปลายทางยังผิดต่อ รอบนี้เป็นวงหมุน 4 เว็บ ไม่ใช่สลับคู่
รอบก่อนแก้เป็นสลับคู่ (pairwise) แต่ผู้ใช้แจ้งต่อว่า Adobe Firefly/Dola AI/Microsoft Designer/
Ideogram ยังผิดอยู่ — ภาพลูกศรรอบนี้ชี้เป็น**วงหมุน 4 ทอด** ไม่ใช่คู่สลับ (Firefly→Ideogram→
Microsoft Designer→Dola AI→กลับมา Firefly) แก้โดยไล่ตามลูกศร:
- Microsoft Designer กลับไปใช้ `LOGO_MSFT` (ริบบิ้นม่วง) — คือค่าดั้งเดิมก่อนสลับรอบที่แล้ว
- Adobe Firefly กลับไปใช้ `LOGO_FIREFLY` (ไล่สีรูปตัว S) — คือค่าดั้งเดิมก่อนสลับรอบที่แล้วเช่นกัน
- Ideogram เปลี่ยนเป็น `LOGO_DOLA` (วงกลมดำลายลม)
- Dola AI เปลี่ยนเป็น `LOGO_IDEOGRAM` (ป้าย "Fi" สีแดง)
- Arena AI/Design Arena ไม่แก้เพิ่ม (ผู้ใช้ไม่ได้วงไว้รอบนี้ แปลว่าคู่นั้นถูกแล้วจากรอบก่อน)
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — โลโก้ปลายทาง 6 ใน 13 เว็บสลับผิดคู่กัน (แก้ 3 คู่ที่ผู้ใช้วงไว้)
ผู้ใช้ส่งภาพวงกลม+ลูกศรชี้ว่าโลโก้ 3 คู่สลับกันอยู่ (ตอนจับคู่ไฟล์ภาพกับปลายทางรอบก่อนเดาจากรูปทรง/
สีเอาเองเพราะไม่รู้จักแบรนด์เหล่านี้แน่ชัด — ผิดไป 3 คู่):
- Arena AI ↔ Design Arena — สลับ `logo:` ให้ Arena AI ได้ไอคอนเสา (`LOGO_DESIGNARENA`) และ
  Design Arena ได้ laurel wreath (`LOGO_ARENA`)
- Adobe Firefly ↔ Dola AI — สลับให้ Firefly ได้ไอคอนวงกลมดำลายลม (`LOGO_DOLA`) และ Dola AI ได้
  ไอคอนไล่สีรูปตัว S (`LOGO_FIREFLY`)
- Microsoft Designer ↔ Ideogram — สลับให้ Microsoft Designer ได้ป้าย "Fi" สีแดง (`LOGO_IDEOGRAM`)
  และ Ideogram ได้ริบบิ้นม่วง (`LOGO_MSFT`)
- แก้แค่ `logo:` ที่ชี้ไปยังชื่อ const ใน `DESTS` (`60-export.js`) ไม่ต้องแตะไฟล์ภาพ/const เดิมเลย
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — โลโก้ปลายทางในกล่องส่งงาน (08) เปลี่ยนเป็นไฟล์จริงที่ผู้ใช้ส่งมา ครบ 13 เว็บ
ผู้ใช้ส่งไฟล์โลโก้จริงมาให้ 13 ไฟล์ (บาง .webp) เพื่อแทนที่สัญลักษณ์ตีความเองที่ทำไว้ก่อนหน้า (ตอน
ยังไม่มีไฟล์ต้นฉบับจริงในมือ เคยตั้งใจเลี่ยงไว้ด้วยเหตุผลด้านเครื่องหมายการค้า — ตอนนี้มีไฟล์จริง
จากผู้ใช้แล้วจึงฝังตรงๆ):
- **.webp ไม่รองรับใน System.Drawing/.NET** ที่ใช้แปลงไฟล์อื่นๆมาตลอด (74/75/78) — แก้ด้วยการเปิด
  ผ่าน Chrome (Claude_Browser) วาดลง `<canvas>` แล้ว `toDataURL('image/png')` แทน (เบราว์เซอร์
  ถอดรหัส webp ได้เอง) ทำเป็นเว็บเซิร์ฟเวอร์เล็กๆ (`preview_start`) รับไฟล์ผ่าน POST ไปเซฟเป็น PNG
  ให้สคริปต์ทำงานต่อได้ปกติ — ไฟล์ที่ต้องใช้ระหว่างทาง (`.claude/launch.json`, สคริปต์แปลงไฟล์) ลบ
  ออกหลังใช้งานแล้ว ไม่ติดไปกับ commit
- **Google AI Studio/Gemini/ImageFX แยกโลโก้จริงคนละแบบแล้ว** (เดิมทั้ง AI Studio และ ImageFX ใช้
  โลโก้ Google G ร่วมกัน) — เพิ่ม `LOGO_GOOGLE_AISTUDIO`/`LOGO_GOOGLE_IMAGEFX` แยกจาก `LOGO_GEMINI`
- **สาย Arena AI (laurel wreath) ต้นฉบับเป็นเส้นสีเข้มบนพื้นโปร่งใสล้วน ไม่มีป้ายพื้นหลังเป็นของ
  ตัวเอง** — ตรวจแล้วเจอปัญหาเดียวกับบั๊ก `LOGO_OPENAI` ก่อนหน้า (จมหายบนธีมมืด) ก่อนจะฝังจริง จึง
  คอมโพสิตพื้นขาวทับให้ก่อน (`Graphics.DrawImage` ผ่าน GDI+) เห็นชัดคงที่ไม่ขึ้นกับธีมแล้ว
- ย่อทุกไฟล์ลงเหลือ 96×96px ก่อนฝัง (แสดงจริงแค่ 20×20px ใน `.dlogo`) ลดขนาดจากรวม ~1.66MB เหลือ
  ~94KB — ไม่กระทบคุณภาพที่ขนาดแสดงผลจริงเลย
- เพิ่ม CSS `.dcard .dlogo img.realicon{object-fit:contain}` (`styles.css`) คู่กับ `svg` เดิม
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย (ไฟล์โต ~1.98MB→2.10MB) · `node test/run.mjs`
  ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — เปลี่ยนไอคอนมุมกล้อง (05) เป็นชุดใหม่ตามภาพที่ผู้ใช้ส่งมา (รอบ 3)
ผู้ใช้ส่งไฟล์ภาพจริงแยกไฟล์มาให้ทั้ง 8 ไฟล์อีกรอบเพื่อเปลี่ยนสไตล์ไอคอนมุมกล้อง — ต่างจากรอบก่อน
(ที่ครอปจากภาพตารางเดียวเป็นภาพเกือบจัตุรัส) รอบนี้ไฟล์**เป็นสี่เหลี่ยมกว้างพอดี** (~286-291×136-150px
สัดส่วน ~2:1) และไม่มี alpha (JPEG ทึบ) ทำให้ตรงกับสัดส่วนการ์ดในแอปดีขึ้นกว่าเดิมมากโดยไม่ต้อง
ครอปเพิ่ม:
- จับคู่ไฟล์กับความหมายของแต่ละมุมกล้องด้วยเนื้อภาพจริง ไม่ใช่กะสี: รถบัสมุม 3/4 หน้า+ประกาย
  ดาว→`hero34` (มุมฮีโร่), รถบัส 3/4 หน้ามุมเงยเห็นกระจังหน้าเด่น→`low` (มุมต่ำ), รถบัสด้านข้าง
  ตรงเป๊ะ (perpendicular แท้ๆ)→`profile`, รถบัส 3/4 มุมกว้างสีม่วง→`rear34`, โดรน+รถมองจากบน→
  `drone`, กล้อง+คนเดิน+รถบัส→`street`, รถบัส+เส้นความเร็ว+กล้องติดราง→`track`, แว่นขยาย+รูปภาพ
  ในกรอบ→`detail`
- สุ่มสีพื้นจากมุมของแต่ละไฟล์จริงด้วยสคริปต์ (ไม่กะด้วยตา) มาตั้งเป็น `ANGLE_TILE_BG` ใหม่ทั้งชุด
  (`20-chips.js`) แทนค่าเดิม — ยังใช้กลไก `object-fit:contain` + พื้นหลังไล่สีตรงกันเดิมที่ทำไว้
  ก่อนหน้า (ไม่ต้องแก้ CSS เพิ่มเลย เพราะสัดส่วนใหม่ใกล้เคียงคอนเทนเนอร์กว่าเดิมอยู่แล้ว)
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย (ไฟล์โต ~1.97MB→1.98MB) · `node test/run.mjs`
  ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — กรอบตัวเลือกของการ์ดไอคอน (02/05/08) จมอยู่หลังรูปภาพ มองไม่เห็น
ผู้ใช้แจ้งว่า "กรอบตัวเลือกอยู่ด้านหลัง icon" — ตรวจแล้วพบว่าจริง: กรอบสีขาวที่บอกว่าการ์ดถูกเลือก
อยู่ (`outline:2px solid #fff;outline-offset:-3px` บน `.icotop`) ถูก `<img class="realicon">` ที่
ฝังเต็มพื้นที่ `.icotop` (ตั้งแต่เปลี่ยนไอคอน 3 หมวดเป็นภาพจริงก่อนหน้านี้) บังจนมองไม่เห็นเลย —
`outline-offset` ติดลบทำให้กรอบวาด**เข้าไปในตัวกล่อง** ซึ่งเป็นพื้นที่เดียวกับที่รูปภาพครอบอยู่พอดี:
- ย้ายกรอบจาก `.icotop` (พื้นที่ไอคอนอย่างเดียว) ไปที่ปุ่มทั้งใบ (`.chip.card.icocard.tile` ตอน
  `aria-pressed=true`) พร้อมเปลี่ยน `outline-offset` เป็น**บวก** (วาดออกด้านนอกกล่อง) — อยู่นอก
  พื้นที่ของรูปภาพเสมอไม่มีทางถูกทับได้อีก และได้กรอบรอบทั้งการ์ด (ไอคอน+ป้ายชื่อ) ดูสมบูรณ์กว่า
  กรอบเดิมที่ครอบแค่พื้นที่ไอคอนอย่างเดียว
- `outline` ตามมาตรฐานเว็บปัจจุบันโค้งตาม `border-radius` ของกล่องเองอยู่แล้ว ไม่ต้องเพิ่ม CSS
  จัดการความโค้งมุมเพิ่ม
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — ปุ่มซูมโดนการ์ดสายรถเมล์บังตอนสายเยอะ (ย้ายมุมล่างขวา → ล่างซ้าย)
ผู้ใช้แนบภาพวงกลมแดงชี้ว่า "สายรถบัง เครื่องมือซูมกดใช้งานลำบาก" — การ์ด "สายรถเมล์" ลอยมุมขวาบน
ของแผนที่ (`#mapBox .routelegend`) ยืดสูงได้ถึง `calc(100% - 16px)` เมื่อมีหลายสาย (เคสนี้ 7 สาย)
ส่วนปุ่มซูม (`.maptools`) เดิมอยู่มุม**ขวาล่าง** — ที่เดียวกับที่การ์ดสายรถเมล์ยืดลงมาถึงพอดีเมื่อมี
สายมากพอ ทำให้ปุ่ม +/−/เต็มจอ ถูกการ์ดทับบางส่วน กดยาก:
- ย้าย `.maptools` จากมุมขวาล่างไปมุม**ซ้ายล่าง** (`styles.css`) — การ์ดสายรถเมล์ลอยฝั่งขวาเสมอ
  ไม่ว่าจะมีกี่สาย จึงไม่มีวันยืดไปทับปุ่มซูมที่ฝั่งซ้ายได้อีก แก้ปัญหาถาวรไม่ต้องคอยจำกัดความสูง
  การ์ดหรือไล่จัดการทีละความกว้างจอ · ตรวจแล้วว่าโหมดเต็มจอ (`#mapFull`) ไม่กระทบ เพราะการ์ดสาย
  รถเมล์ในโหมดนั้นย้ายไปอยู่แถบด้านล่างแผนที่แยกกันคนละพื้นที่อยู่แล้ว (`#mapFull .fl`)
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — บั๊กร้ายแรง: คลิกสายที่ไม่มี relation id ได้เห็นเส้นทางของสายอื่นที่ไม่เกี่ยวกัน
ผู้ใช้แจ้งอีกครั้งว่าเส้นทางที่แสดง "ไม่ตรงกับสายรถ" (สาย "13 AC (3-38)" ที่เซ็นทรัลเวิลด์) — ตาม
รอยจนเจอต้นเหตุจริงที่ลึกกว่าที่แก้ไปรอบก่อน (การ์ดระยะ 8 กม.) เยอะ:
- **overpass-api.de บล็อก origin `*.github.io`** (ปัญหาเดิมที่เจอกับฐานถนนตอนทำ MAP_SEED) ทำให้
  เว็บที่ deploy จริงดึงข้อมูลสายรถเมล์ที่จอดป้าย (stage B) สดไม่สำเร็จ**เสมอ** ต้องถอยไปใช้
  `ROUTE_SEED` (`10b-route-seed.js`) แทน — และ `ROUTE_SEED` **ไม่มี relation id ติดมาตั้งใจ**
  (ตามที่บันทึกไว้แต่แรกว่า "การระบายสีเส้นทางรายสายต้องมาจาก live fetch stage C เท่านั้น")
  ทุกกลุ่มสายที่ตกมาทางนี้จึงได้ `g.ids=[null,null]` เหมือนกันหมด — ไม่ใช่แค่บางสาย
- **`geoKeyOf(ids)` เดิมเอา `ids` ไป `.join(",")` ตรงๆ** — `[null,null].join(",")` ใน JS ได้ค่า
  ว่างเปล่า (`""`) ไม่ใช่ `"null,null"` เพราะ `join()` แปลง `null`/`undefined` เป็นสตริงว่าง **ทุก
  สายที่ไม่มี relation id จึงได้ cache key เดียวกันหมดทั้งแอป ข้ามสายข้ามโลเคชั่น** สายแรกที่ดึง
  geometry สำเร็จ (ผ่าน `ROUTE_GEOM_SEED` ใหม่) จะไปแคชทับคีย์นั้น แล้วสายอื่นๆที่คลิก "ดูทั้งสาย"
  ตามมาทีหลัง (ที่จริงไม่มี geometry ให้เลย) กลับ**เห็นเส้นทางของสายแรกที่แคชไว้ ไม่ใช่ของตัวเอง**
  — เพราะ `loadRouteGeom()` เช็ก cache (`geoRecall`) ก่อนเช็ก seed/proximity guard เสมอ ยิ่งไปกว่า
  นั้นค่านี้ยัง**ถูกบันทึกถาวรใน localStorage** (`GEO_KEY`) ทำให้บั๊กนี้ค้างข้ามเซสชันของผู้ใช้จริงด้วย
- แก้ที่ `geoKeyOf()` เอง (`56-route-geom.js`) — เปลี่ยนให้รับกลุ่ม (`g`) ทั้งก้อนแทน `g.ids` เดี่ยวๆ
  เมื่อ `ids` เป็นเลขจริงทุกตัวยังคีย์แบบเดิม (แชร์แคชข้ามโลเคชั่นได้เหมือนก่อน ไม่เปลี่ยนพฤติกรรม)
  แต่เมื่อ `ids` ใช้ไม่ได้ (null หรือไม่ใช่เลข) ใช้ `"ref:"+normRef(g.ref)` แทน — แยกคีย์ต่อสายจริง
  ไม่มีวันชนกันข้ามสายอีก แก้ 4 จุดที่เรียก `geoKeyOf()` ให้ส่ง `g` ทั้งก้อน (`51-map-render.js` ×2,
  `56-route-geom.js`, `57-route-view.js`)
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์
- **ผู้ใช้ที่เคยโดนบั๊กนี้ไปแล้ว**: ค่าที่แคชผิดไว้ใน localStorage เครื่องนั้นจะยังค้างอยู่จนกว่าจะกด
  ปุ่ม "ล้างข้อมูลเส้นทางที่เก็บไว้" (ล้าง `as3d_geo` ทั้งหมด) — คีย์เก่าที่ผิด (`""`) จะไม่ถูกใช้อีก
  เพราะโค้ดใหม่ไม่มีทางสร้างคีย์นั้นได้แล้ว แต่จะยังนอนเปล่าประโยชน์อยู่ใน storage จนกว่าจะล้าง

## v41 (กำลังทำ) — เส้นทางที่ seed ให้บางสายแสดงผลไม่ตรงจุด ผิดสาย (แก้ที่ปลายทางแบบถาวร)
ผู้ใช้แจ้งว่าเส้นทางเดินรถที่แสดง "ไม่ตรงสาย แสดงผลไม่ตรงโลเคชั่น" — ตรวจสอบ 248 คู่ (สาย×โลเคชั่น)
ในรายการที่ตรวจสอบแล้วทั้ง 22 จุด เทียบกับ geometry จริงในสแนปช็อตพบสายที่เข้าเงื่อนไข "จุดที่ใกล้
โลเคชั่นที่สุดในเส้นทางยังห่างเกิน 8-11 กม." อยู่จริง 3 คู่ — เด่นที่สุดคือ **"A3"** (ใช้ที่
CentralWorld/ราชดำริ): ไฟล์ Google My Maps ต้นฉบับระบุต้นทาง-ปลายทางเป็น "ท่าอากาศยานดอนเมือง →
สวนลุมพินี" (ทั้งสองอยู่กรุงเทพฯตอนกลาง-เหนือ) แต่ geometry ที่วาดไว้จริงอยู่แถวบางนา/สุวรรณภูมิ
(กรุงเทพฯตะวันออก-ใต้) ห่างจากคำอธิบายตัวเองเป็นสิบกิโล — เป็นความผิดพลาดในการวาดเส้นของไฟล์
ต้นฉบับที่คนทำแผนที่คงพลาดสายที่ถูกไปวางเส้นผิด (188 สายมากเกินจะตรวจด้วยมือให้ครบทุกสาย):
- **แก้ที่ปลายทางแทนไล่จับทีละสาย** — เพิ่ม `seedNearPin()` ใน `56-route-geom.js` ตรวจว่า
  เส้นทางที่ seed จับคู่ได้มีจุดใดจุดหนึ่งอยู่ในรัศมี 8 กม. จากหมุดที่กำลังดูอยู่ (`MAPD.at`) จริง
  ก่อนยอมใช้ — ไกลกว่านั้นถือว่าน่าจะข้อมูลผิดสาย/ผิดจุด ปฏิเสธแล้วถอยไป cache/live ตามปกติทันที
  (ปลอดภัยกว่าเชื่อ seed แบบไม่มีเงื่อนไข ป้องกันได้ทุกสายที่มีปัญหาแบบเดียวกัน ไม่ใช่แค่ A3 ที่พบ)
- เลือก 8 กม. จากการตรวจสายที่ห่างจริงแต่ยังสมเหตุสมผล (145/513/547/3-23E ห่างสุด ~3.6-4.5 กม.
  ยังผ่านได้ปกติ — น่าจะเป็นแค่ความโค้งของถนนจริง ไม่ใช่ข้อมูลผิด) เทียบกับสายที่ผิดจริง (A3/3-25E
  ห่าง 10-12 กม. — ถูกปฏิเสธ) มีช่องว่างเผื่อพอสมควรระหว่างสองกลุ่มนี้
- ผลจากการตรวจ 248 คู่: ใช้ seed ได้จริงหลังผ่านการ์ดนี้ 178 คู่ (จาก 181 คู่ที่จับคู่ชื่อได้ก่อนมี
  การ์ด — ปฏิเสธไป 3 คู่ที่ผิดจริง) ไม่กระทบสายอื่นที่ถูกต้องอยู่แล้วเลย
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์ ·
  สคริปต์ตรวจสอบยืนยันว่า A3/3-25E ถูกปฏิเสธแล้ว ส่วนสายที่ถูกต้อง (145/513/547/3-23E ฯลฯ) ยังผ่าน

## v41 (กำลังทำ) — แก้ applyRouteGeomSeed() ไม่จับคู่เมื่อ g.ref เป็น "เก่า (ใหม่)" รวมกัน
ทดสอบบนเว็บจริงหลังคอมมิตก่อนหน้าพบว่า seed ยังไม่ถูกใช้จริงตอนคลิก "ดูทั้งสาย" — ตรวจดูจึงเจอว่า
`g.ref` ที่ได้จาก tag `ref` ของ OSM สำหรับกรุงเทพฯมักเป็นสตริงรวม "เก่า (ใหม่)" เช่น `"17 (4-3)"`
ตรงกับรูปแบบเดียวกับที่ `LOCATIONS[].bus` ใช้ (`curatedLabel()` แยกด้วยวิธีนี้อยู่แล้ว) แต่
`applyRouteGeomSeed()` รอบก่อนเอาทั้งสตริงไป `normRef()` ตรงๆ ได้ `"17(4-3)"` ซึ่งไม่ตรงกับ key
ไหนในสแนปช็อตเลย (ROUTE_GEOM_SEED เก็บแยกเป็น `"17"`/`"4-3"` คนละ key) — จับคู่ไม่ติดสักสายเดียว:
- แก้ให้ตัดวงเล็บออกแล้ว split ด้วยช่องว่างก่อน (`.replace(/[()]/g,"").split(/\s+/)`) เหมือนวิธีที่
  `curatedLabel()` ใช้อยู่แล้ว จากนั้นลองจับคู่ทีละส่วน — ทดสอบกับสาทร (8 สาย) พบว่าจับคู่ได้ 6/8
  สาย (17→4-3, 77, 149→4-53, 167→4-26, 1-32E, 3-52 — เหลือ 21E/402 ที่ไม่มีในสแนปช็อตจริงๆ)
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์ · ตรวจ
  บนเว็บจริงอีกรอบว่า seed ถูกใช้จริงแล้ว (ดูทั้งสายสำเร็จทันทีไม่มี toast รอเน็ต)

## v41 (กำลังทำ) — สแนปช็อตเส้นทางเดินรถเต็มสาย 188 สายจาก Google My Maps ของผู้ใช้เอง
ผู้ใช้ส่งลิงก์ Google My Maps ที่ทำเอง (mid=1G-ARTn7RLH7FaObR0ndKWAr9E-EQ0hI) ขอให้เพิ่มข้อมูล
แผนที่และสายรถจากไฟล์นี้ — เปิดดูพบว่าเป็นแผนที่เส้นทางรถเมล์กรุงเทพฯที่ทำเองไว้ละเอียดมาก
(189 เส้นทาง + MRT สายสีน้ำเงิน/สีม่วง 2 เส้น) นี่คือข้อมูลที่ "ดูทั้งสาย" (เฟส 4 ของแผนเดิม)
ต้องพึ่ง Overpass สดมาตลอด และเจอ CORS/rate-limit แบบเดียวกับที่ MAP_SEED แก้ให้กับฐานถนนไปแล้ว
จึงใช้แนวทางเดียวกัน: ดึงมาเก็บเป็นสแนปช็อตในเครื่อง ไม่ต้องพึ่งเน็ตเลยตอนใช้งานจริง:
- ดึง KML ของแผนที่ตรงๆด้วย `Invoke-WebRequest` (server-side ไม่ติด CORS เหมือนที่เคยทำกับ
  Overpass) ได้ไฟล์ 1.6MB แยก placemark ทั้ง 189 เส้นด้วย regex (ไม่ใช้ XML parser เพิ่ม
  dependency) ทำ radial decimation ที่ tolerance 8 ม. — **เท่ากับ** `simplifyWay(pts,8)` ที่
  `56-route-geom.js` ใช้กับข้อมูลสดจาก Overpass อยู่แล้ว ให้ความละเอียดใกล้เคียงกัน (จุดจาก KML
  ถูกย่อไว้ล่วงหน้าอยู่แล้วตั้งแต่ต้นทาง เหลือแทบไม่ต้องตัดอะไรเพิ่ม 7737→7734 จุด)
- ชื่อเส้นทางในไฟล์มีทั้งเบอร์เก่า ("สาย 26") และเบอร์ใหม่คู่เบอร์เก่าในวงเล็บ ("สาย ปอ.4-26(167)")
  — เก็บทั้งสองเบอร์เป็นคนละ key ชี้กลับมาข้อมูลเดียวกันผ่าน `ROUTE_GEOM_ALIAS` ให้ค้นเจอไม่ว่า
  `g.ref` ที่ได้จาก Overpass สดจะเป็นเบอร์ไหน (ยกเว้น 2 กรณีที่ทั้งเบอร์เก่าเดี่ยวๆ "2"/"8" และ
  แบบมีวงเล็บอ้างเบอร์เก่าเดียวกันมีอยู่ในไฟล์พร้อมกัน — ให้ป้ายเบอร์เดี่ยวๆเป็นหลัก ไม่ทับกัน)
- ไฟล์ใหม่ `src/js/10d-route-geom-seed.js` (แทรกใน `_order.json` ต่อจาก `10c-map-seed.js`) —
  `ROUTE_GEOM_SEED_DATE`/`ROUTE_GEOM_SEED` (188 สาย)/`ROUTE_GEOM_ALIAS` (84 เบอร์ทางเลือก)
  ~197 KB เก็บพิกัดเป็น `[lat,lon]` เหมือน `MAP_SEED`
- `56-route-geom.js` เพิ่ม `applyRouteGeomSeed(ref)` และเรียกก่อน cache/live เสมอใน
  `loadRouteGeom()` — สาย/ตำแหน่งที่อยู่ในสแนปช็อตนี้กด "ดูทั้งสาย" แล้วเห็นทันที ไม่ต้องรอเน็ต
  เลย · `51-map-render.js` ปุ่ม "ดูทั้งสาย" ก็เช็ก seed นี้ด้วยตอนตัดสินใจว่าจะโชว์ "(ดึงข้อมูล)"
  หรือไม่ ไม่ให้ดูเหมือนต้องรอทั้งที่จริงมีอยู่แล้ว
- **ไม่ได้ใส่ MRT สายสีน้ำเงิน/สีม่วง** — แอปนี้ยังไม่มี UI แสดงเส้นทางรถไฟฟ้า (ระบบ `LOCATIONS[].bus`
  เป็นลิสต์รถเมล์ล้วน) ใส่ไว้เฉยๆจะเป็นข้อมูลที่ไม่มีระบบไหนอ้างถึง ถ้าต้องการฟีเจอร์นี้ต้องเพิ่ม UI
  แสดงเส้นทางรถไฟแยกเป็นงานใหม่
- **แก้ `test/09-routegeom.test.js`** — เทสต์เดิมใช้ ref ปลอม "A1" ซึ่งชนกับสายรถตู้สนามบินจริง
  ("สาย A1") ในสแนปช็อตใหม่ ทำให้ seed ตัดหน้า Overpass ก่อนเทสต์จะทันวัดพฤติกรรมดึงสด (ผลลัพธ์
  ถูกต้องแล้วในเชิงพฤติกรรมจริง — แค่เทสต์ไม่ได้เดินตามเส้นทางที่ตั้งใจวัดอีกต่อไป) เปลี่ยนเป็น
  "ZT1/ZT2/ZT3" ที่ไม่มีทางชนกับเลขสายจริง
- ตรวจแล้ว: `node build.mjs --check` ตรงทุกไบต์ทั้งสองเป้าหมาย (ไฟล์โต ~1.82MB→1.97MB) ·
  `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — แก้โลโก้ ChatGPT ที่มองไม่เห็นในธีมมืด
ผู้ใช้แนบภาพวงกลมสีแดงชี้ตำแหน่งโลโก้ ChatGPT ในกล่องส่งงาน (08) ว่ามองไม่ชัดในธีมมืด — ต้นเหตุคือ
`LOGO_OPENAI` (`60-export.js`) เป็นแค่เส้น `stroke="#111"` ไม่มีพื้นหลังของตัวเอง ต่างจากโลโก้อื่น
ทุกตัวที่มีป้ายพื้นหลังสีทึบเป็นของตัวเอง (เหมือนไอคอนแอปมือถือ) ทำให้เห็นชัดคงที่ไม่ขึ้นกับธีม —
เส้นสีเข้มเกือบดำจึงจมหายไปกับพื้นหลังการ์ด `.dcard` ที่เข้มอยู่แล้วในธีมมืด (ในธีมสว่างพอเห็นลางๆ
เพราะพื้นการ์ดสว่างกว่า):
- เพิ่มป้ายพื้นหลังดำ (`#0D0D0D`) ให้ `LOGO_OPENAI` เหมือนโลโก้อื่น แล้วเปลี่ยนเส้น/จุดกลางเป็นสีขาว
  — ตอนนี้เป็นป้ายทึบสีตัวเอง ไม่ขึ้นกับสีพื้นหลัง `.dcard` ของธีมไหนเลย เห็นชัดทั้งสองธีมแน่นอน
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — ไอคอนสไตล์การเรนเดอร์ (08) เป็นภาพจริงจากผู้ใช้ ครบทั้ง 9 ไอคอนแล้ว
ผู้ใช้ส่งไฟล์ภาพจริงแยกไฟล์มาให้ทั้ง 9 ไฟล์ตรงกับ `STYLE_ICONS` ทุกตัว (ue5, octane, hdr, hyper,
auto, clean, cine, ray, trails) — ครบทั้ง 3 หมวดไอคอนของแอปแล้ว (02/05/08 ทั้งหมดเป็นภาพจริงแล้ว
ไม่มี SVG วาดมือเหลืออีก):
- ตรวจไฟล์ก่อนฝังพบว่า**มีขอบมุมโค้งโปร่งใสจริง** (alpha=0 ที่มุม ตรวจด้วยสคริปต์ ไม่ใช่กะด้วยตา) —
  ต่างจาก 02/05 ที่เป็นไฟล์ทึบ/ครอปจากตารางเดียว จึง**flatten พื้นหลังไล่สี 135deg ทับไฟล์ก่อนเซฟ**
  (สุ่มสีจริงจากภาพแต่ละใบด้วยสคริปต์ เหมือนวิธีที่ใช้กับ `ANGLE_TILE_BG`) แล้วเซฟเป็น JPEG q90 —
  ได้ไฟล์เล็กลงมาก (~10-16KB/ไอคอน จาก PNG โปร่งใสเดิม ~90-120KB/ไอคอน)
- `STYLE_ICONS` (`src/js/20-chips.js`) เปลี่ยนจาก SVG เป็น `<img class="realicon"
  src="data:image/jpeg;base64,...">` เหมือน `VEHICLE_ICONS`/`ANGLE_ICONS`
- เพิ่ม `STYLE_TILE_BG` (สีที่ flatten ไว้ในไฟล์แต่ละใบพอดี) ให้ `styleCardHTML()` ส่งเป็น
  `bgOverride` ให้ `tileHTML()` เหมือน `angleCardHTML()` — พื้นหลังการ์ดตรงกับภาพเนียนสนิท
- เพิ่ม `.icocard.stylecard .icotop img.realicon{object-fit:contain}` (`styles.css`) — ภาพเกือบ
  จัตุรัสเหมือนหมวดมุมกล้อง ถ้าใช้ cover จะครอปดาว UE5/จุดแฟลชกล้อง HDR ที่อยู่ใกล้ขอบบนขาดเหมือน
  ปัญหาที่เจอกับ 05 ก่อนหน้า จึงใช้ contain ตั้งแต่แรกไม่ต้องรอผู้ใช้แจ้งซ้ำ
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย (ไฟล์โต ~1.7MB→1.82MB) · `node test/run.mjs`
  ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — เพิ่มโลโก้ปลายทางในกล่อง "คัดลอกคำสั่ง & ส่งงานเข้าเว็บ AI" (08)
ผู้ใช้แนบภาพหน้าจอวงกลมสีแดงชี้ตำแหน่งข้างชื่อปลายทางแต่ละเว็บใน modal ส่งงาน (13 ปลายทาง) พร้อม
ขอ "เพิ่ม logo ด้านหลังชื่อเว็บของแต่ละที่... เพื่อความสวยงามและดูง่ายขึ้น":
- เพิ่มโลโก้ย่อ (SVG ขนาด 20×20px) หน้าชื่อทุกปลายทางทั้ง 13 เว็บใน `.dcard` — จัดเป็นแถว
  `.dtitle` (โลโก้+ชื่อ) แทนที่ `<b>` เดี่ยวๆ เดิม (`60-export.js`, `styles.css`)
- **วาดเป็นสัญลักษณ์แบบตีความเอง ไม่ใช่ก็อปปี้ไฟล์โลโก้จริงของแต่ละบริษัท** — ตามแนวทางเดียวกับ
  ไอคอน "ue5" ในหมวดสไตล์เรนเดอร์ที่ทำไว้ก่อนหน้า (วงกลม+ตัว U ไม่ใช่การจำลองโลโก้ Unreal Engine
  ตรงๆ) เพื่อเลี่ยงความเสี่ยงด้านการใช้เครื่องหมายการค้าของผู้อื่นโดยไม่มีไฟล์ต้นฉบับจริงในมือ:
  Google/Google Gemini/ImageFX ใช้ตัว G 4 สีและประกายดาวไล่สีตามจริง (รูปทรงเรขาคณิตพื้นฐาน
  ไม่มีองค์ประกอบสร้างสรรค์ซับซ้อนพอจะถือเป็นการละเมิด), Microsoft Designer ใช้ 4 สี่เหลี่ยมสี
  (รูปทรงเรียบง่ายเช่นกัน), ที่เหลือ (ChatGPT/Bing/Leonardo/Ideogram/Hugging Face/Arena AI/
  Adobe Firefly/Design Arena/Dola AI) เป็นสัญลักษณ์ตีความเองล้วน (วงแหวนซ้อน, ตัวอักษรย่อในวงกลม
  ไล่สี, หน้ายิ้ม HuggingFace, ประกายหิ่งห้อยสีแดง Adobe ฯลฯ) ไม่ใช่การจำลองโลโก้จริง
- ถ้าผู้ใช้มีไฟล์โลโก้ทางการจริงอยากใช้แทน ส่งไฟล์มาได้เหมือนขั้นตอน `VEHICLE_ICONS`/`ANGLE_ICONS`
  ก่อนหน้า — ฝังเป็น base64 แทนที่ SVG ตีความเองได้ทันที
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — แก้ไอคอนมุมกล้อง (05) โดนครอปขอบบนขาด (หลังคารถ/ใบพัดโดรนหาย)
ผู้ใช้แจ้งว่าไอคอนมุมกล้องรอบก่อน (แก้ขอบดำไปแล้ว) ตอนนี้ขอบบนของไอคอนขาดหายไป (หลังคารถ/เสา
อากาศ/ใบพัดโดรนถูกตัด) — ต้นเหตุคือ `.icotop` ของการ์ดในแอปเป็นกล่องแบนกว้างมาก (สูงแค่ 56px
กว้างตามคอลัมน์การ์ด ~150-200px, อัตราส่วน ~3:1) แต่ภาพจริงที่ครอปมาเกือบเป็นจัตุรัส (~1:1)
`object-fit:cover` เดิม (ใช้ร่วมกับ `VEHICLE_ICONS` ที่ภาพเป็นแนวนอนอยู่แล้วจึงไม่มีปัญหา) ต้อง
ขยายภาพตามความกว้างแล้วครอปความสูงทิ้งเกือบครึ่งเพื่อให้เต็มกล่อง ตัดเนื้อหาไอคอนจริงที่อยู่ใกล้
ขอบบน (หลังคา/ใบพัด) ทิ้งไปด้วย:
- เปลี่ยน `.icocard.anglecard .icotop img.realicon` เป็น **`object-fit:contain`** (`styles.css`) —
  เห็นไอคอนเต็มภาพเสมอ ไม่มีวันโดนครอปอีกไม่ว่าอัตราส่วนการ์ดจะเป็นเท่าไร
- ปัญหาที่ตามมาจาก `contain`: จะเหลือแถบว่างซ้าย-ขวาให้เห็นพื้นหลัง `.icotop` เดิม ซึ่งไล่สีตามลำดับ
  แสดงผล (`tileGrad(idx)`) ไม่ตรงกับสีพื้นจริงในภาพแต่ละใบ (เช่นภาพโดรนพื้นแดง แต่ index อาจได้
  โทนน้ำเงิน) จะเห็นรอยต่อสีชัดเจน — แก้ด้วยการดึงสีจริงจากมุมภาพแต่ละใบ (สุ่มพิกเซลมุมบนซ้าย/
  ล่างขวาของไฟล์ครอปจริงด้วยสคริปต์ ไม่ใช่กะด้วยตา) มาตั้งเป็น `ANGLE_TILE_BG` (`20-chips.js`)
  แทน `tileGrad(idx)` เฉพาะการ์ดมุมกล้อง — ไล่สีพื้น `.icotop` (ทิศทาง 135deg ตรงกับภาพต้นฉบับ)
  จึงเนียนสนิทกับสีในภาพไม่ว่าจะเหลือขอบว่างด้านไหนก็ตาม
- `tileHTML()` เพิ่มพารามิเตอร์ที่ 7 (`bgOverride`) รับคู่สีมาแทน `tileGrad(idx)` เมื่อมีให้ —
  `vehicleCardHTML()`/`styleCardHTML()` ไม่ส่งพารามิเตอร์นี้ จึงพฤติกรรมเดิมทุกอย่างไม่เปลี่ยน
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — แก้ขอบดำที่หลุดมาในไอคอนมุมกล้อง (05) จากการครอปรอบก่อน
ผู้ใช้ส่งภาพซูมชี้ว่าไอคอนมุมกล้องที่ฝังไปรอบก่อน (คอมมิตก่อนหน้า) ยังเห็นเศษพื้นหลังสีเข้ม/
เส้นกริดของภาพต้นทางหลุดมาที่มุมของไทล์ ("ขอบดำไม่สวยงาม ภาพเอียง") — ต้นเหตุคือแต่ละช่องใน
ภาพตาราง 42 ต้นฉบับเป็น**สี่เหลี่ยมมุมโค้ง** วางบนพื้นหลังกริดเข้ม ไม่ใช่สี่เหลี่ยมเต็มขอบ ตอนตรวจ
ขอบเขตด้วยความสว่างพิกเซล (คอมมิตก่อนหน้า) ได้กรอบสี่เหลี่ยมที่ชิดขอบไทล์พอดี ทำให้ 4 มุมของ
กรอบสี่เหลี่ยมนั้นตกอยู่นอกมุมโค้งจริง กลายเป็นเศษพื้นหลัง/เส้นกริดเฉียงๆติดมาที่มุมภาพ:
- ครอปใหม่โดย **inset เข้าไปอีก 18px รอบกรอบเดิมทุกด้าน** (จาก ~240×233px เหลือ ~206×197px) —
  มากกว่ารัศมีมุมโค้งของไทล์ต้นฉบับ จึงไม่มีพิกเซลพื้นหลังหลุดเข้ามาที่มุมภาพอีก ตรวจแล้วทั้ง 8 ไฟล์
  ก่อนฝัง ไม่มีเศษพื้นหลัง/เส้นกริดเหลือที่มุมไหนเลย
- สัดส่วนภาพเปลี่ยนจาก ~1.038:1 เป็น ~1.045:1 (แทบไม่ต่างจากเดิม) การแสดงผลใน `.icotop` ผ่าน
  `object-fit:cover` จึงยังเต็มพื้นที่การ์ดเหมือนเดิม ไม่ต้องแก้ CSS เพิ่ม
- ไฟล์เล็กลงเล็กน้อยด้วย (~6-8KB/ไอคอน จาก ~9KB) เพราะพื้นที่ภาพเล็กลง
- ตรวจแล้ว: `node build.mjs` สำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — ไอคอนมุมกล้อง (05) เป็นภาพจริงจากผู้ใช้ ครอปจากภาพตารางเดียว
ผู้ใช้ส่งภาพตาราง 4×2 ช่อง (ไอคอน 8 มุมกล้องรวมในไฟล์เดียว) ถามว่าใช้ทำไอคอนมุมกล้องได้ไหม
และขอให้แยกไฟล์ให้ — ตรวจตำแหน่ง/สีของแต่ละช่องเทียบกับความหมายใน `ANGLES` (`10-data.js`) แล้ว
จับคู่ตรงกับลำดับที่ปรากฏในตาราง (แถวบน→ล่าง, ซ้าย→ขวา) พอดีทั้ง 8 ตัว: รถ 3/4 หน้า=hero34,
กล้อง+บัสมองจากมุมต่ำ=low, บัสด้านข้างเต็มคัน=profile, บัส+กล้องมุม 3/4 หลัง=rear34,
โดรน+รถมองจากบน=drone, กล้อง+คนเดิน+บัสระดับสายตา=street, บัส+เส้นความเร็ว+ผู้ถ่ายวิ่งตาม=track,
แว่นขยาย+กราฟิกค้นหา=detail (ซูมดูรายละเอียดลายกราฟิก):
- หาขอบเขตแต่ละช่องด้วยสคริปต์ตรวจความสว่างพิกเซลแบบสแกนแถว/คอลัมน์ (ไม่ได้กะด้วยตา) แล้วครอป
  ด้วย `System.Drawing` (.NET ผ่าน PowerShell) ได้ 8 ไฟล์ขนาด ~240×233px ส่งให้ผู้ใช้ตรวจก่อน 1 รอบ
- **บันทึกเป็น JPEG คุณภาพ 88 ไม่ใช่ PNG** — ภาพต้นทางเป็น .jpg มี noise จากการบีบอัดอยู่แล้ว ครอป
  เป็น PNG (lossless) จะเก็บ noise นั้นไว้หมดทำให้ไฟล์ใหญ่ผิดปกติ (~75KB/ไอคอน ทดสอบแล้ว) เทียบกับ
  JPEG ที่ได้ ~8-10KB/ไอคอนโดยคุณภาพที่เห็นแทบไม่ต่างกันที่ขนาดแสดงผลจริงในแอป (~56px สูง)
- `ANGLE_ICONS` (`src/js/20-chips.js`) เปลี่ยนจาก SVG ไดอะแกรมคงที่ (คอมมิตก่อนหน้า) เป็น `<img
  class="realicon" src="data:image/jpeg;base64,...">` เหมือนแนวทางเดียวกับ `VEHICLE_ICONS` — ใช้
  CSS rule `.icocard .icotop img.realicon` เดิมที่มีอยู่แล้ว ไม่ต้องเพิ่ม CSS ใหม่เลย
- ขนาดไฟล์โต ~90 KB (8 ภาพ × ~9KB เฉลี่ยหลังเข้ารหัส base64) จาก ~1.6 MB เป็น ~1.68 MB
- ยังเหลือ 08 · Render Stack (9 สไตล์) ที่ยังเป็น SVG วาดมือ — รอไฟล์ภาพจริงแบบเดียวกัน
- ตรวจแล้ว: `node build.mjs` สร้างสำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — ไอคอนประเภทสื่อ (02) เป็นภาพจริงจากผู้ใช้ ไม่ใช่ SVG วาดมือ
ผู้ใช้ถามตรงๆ ว่าทำไมไอคอนที่วาดเองด้วย SVG (คอมมิตก่อนหน้า) ยังไม่เหมือนภาพตัวอย่างสักที —
คำตอบคือวิธีทำต่างกันโดยพื้นฐาน (พิมพ์พิกัดเส้นด้วยมือ เทียบกับภาพประกอบสีสัน/เงาแบบมืออาชีพ)
และไม่มีเครื่องมือสร้างภาพจากคำบรรยายให้ใช้ในเซสชันนี้ (ตรวจสอบแล้ว มีแต่เครื่องมือแก้ไขภาพที่มีอยู่
กับตัวสร้างหน้าเอกสาร Canva เท่านั้น) จึงถามผู้ใช้ว่ามีไฟล์ภาพต้นฉบับหรือไม่ — **ผู้ใช้ส่งไฟล์จริงมา
ทั้ง 7 ไฟล์** (PNG พื้นหลังสีสัน ตรงกับ id ใน `VEHICLES` ทุกตัว: wrap, dd, bus3d, shelter, muvmi,
billboard, ledscreen) จึงฝังภาพจริงแทน SVG เส้นทันที แก้ปัญหาความไม่ตรงกันที่ต้นเหตุ:
- `VEHICLE_ICONS` (`src/js/20-chips.js`) เปลี่ยนจาก SVG `currentColor` เป็น `<img class="realicon"
  src="data:image/png;base64,...">` ฝังไฟล์จริงที่ผู้ใช้ส่งมาโดยตรง — ได้ภาพตรงกับตัวอย่างเป๊ะ 100%
  ไม่ใช่การประมาณด้วยมืออีกต่อไป
- เพิ่ม CSS `.icocard .icotop img.realicon{position:absolute;inset:0;object-fit:cover}` ให้ภาพเต็ม
  พื้นที่ไอคอนบนการ์ด (`styles.css`) — ปุ่มติ๊กถูก (`.tick`) มาทีหลังใน DOM จึงลอยทับภาพได้เองไม่ต้อง
  ตั้ง z-index เพิ่ม
- ขนาดไฟล์โต ~190 KB (7 ภาพ × ~27 KB หลังเข้ารหัส base64) จาก ~1.4 MB เป็น ~1.6 MB — ยังเป็นไฟล์
  เดียวจบ ไม่เพิ่ม dependency ใดๆ
- ยังไม่ได้ทำ 05 · Camera และ 08 · Render Stack เพราะผู้ใช้ยังไม่ได้ส่งไฟล์ภาพจริงของสองหมวดนั้น —
  ถ้าจะให้ตรงเป๊ะเหมือนกันต้องขอไฟล์เพิ่ม ไม่งั้นยังเป็น SVG วาดมือแบบเดิม
- ตรวจแล้ว: `node build.mjs` สร้างสำเร็จทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — MAP_SEED ครบทั้ง 22 โลเคชั่นแล้ว
ผู้ใช้แนบภาพ 5 จุด (Plan B Tower, ถนนราชดำริ, ถนนพระรามที่ 1, เซ็นทรัลเวิลด์) ที่ยังขึ้นแค่จุดหมุด
ไม่มีถนน — ตรงกับ 4 ใน 7 จุดที่ระบุไว้แล้วในรอบก่อนหน้าว่าติด rate-limit ของ overpass-api.de ตอน
ดึงข้อมูลรอบแรก ลองดึงซ้ำอีกครั้ง (ผ่านไป 1 วันจากรอบแรก) คราวนี้**สำเร็จครบทั้ง 7 จุดที่เหลือ**
(planb, chatuchak, centralworld, ratchadamri, rama1, expressway, motorway — รวมถึง "มอเตอร์เวย์"
ที่เป็นจุดตั้งต้นของปัญหานี้ตั้งแต่แรก) ยืนยันว่า rate-limit เป็นแบบชั่วคราวจริงตามที่บอกไว้ก่อนหน้า
ไม่ใช่การบล็อกถาวร:
- `MAP_SEED` (`src/js/10c-map-seed.js`) ตอนนี้ครอบคลุม**ครบทั้ง 22 โลเคชั่น** ที่มีในแอป จาก 15 จุด
  เดิม — ไฟล์ใหญ่ขึ้นจาก ~700 KB เป็น ~991 KB (จุดใหม่ 7 จุดรวมกัน ~2.1 MB ก่อนบีบอัด, บีบด้วย
  Douglas-Peucker+เก็บพิกัดเป็นคู่ตัวเลขเหมือน 15 จุดแรกทุกอย่าง)
- อัปเดต `MAP_SEED_DATE` เป็นวันที่ดึงรอบสุดท้าย (2026-09-21) — ป้าย "ฐานถนนจากสแนปช็อต" บนแผนที่
  จึงบอกวันที่ถูกต้องสำหรับข้อมูลชุดใหม่ทั้งหมด (ไม่ได้แยกวันที่ต่อจุด เพื่อความง่าย)
- ไม่ต้องแก้โค้ด `applyMapSeed()`/`applySeedOrCache()`/`drawMap()` เลยแม้แต่บรรทัดเดียว —
  เป็นแค่การเติมข้อมูลในไฟล์เดิมที่ต่อไว้แล้วตั้งแต่รอบก่อน
- ตรวจแล้ว: `node build.mjs --check` ตรงทุกไบต์ทั้งสองเป้าหมาย (ไฟล์ใหญ่ขึ้นเป็น ~1.3-1.4 MB) ·
  `node test/run.mjs` ผ่านครบ 10 ไฟล์
- **ตอนนี้ไม่มีโลเคชั่นไหนเหลือที่ต้องพึ่ง `drawPointOnly()` อีกแล้ว** — ทุกจุดในแอปมีฐานถนนจริง
  ให้แสดงทันทีไม่ว่าจะเปิดจาก `file://` หรือเว็บที่ deploy จริงที่ไหนก็ตาม

## v41 (กำลังทำ) — ไอคอนใหม่ตามภาพตัวอย่างเป๊ะๆ: ประเภทสื่อ (02) / มุมกล้อง (05) / สไตล์ (08)
ผู้ใช้ส่งภาพสกรีนช็อต 3 ภาพ (แผง 05 · Camera, 02 · Media, 08 · Render Stack ของแอปเวอร์ชันที่
ปรับสีไทล์ไปแล้วก่อนหน้านี้ แต่ไอคอนละเอียดกว่าที่ทำไว้จริง) พร้อมคำสั่ง "ใช้ icon ตามนี้เลย
ไม่แก้ไข" — วาดไอคอนใหม่ทั้ง 24 ตัวให้ตรงกับภาพตัวอย่างมากที่สุดโดยไม่แตะโครง/สี/เลย์เอาต์ไทล์ที่
ถูกต้องอยู่แล้ว (คงเป็นสีไล่เฉด + ไอคอนขาวตรงกลาง + ป้ายชื่อแถบทึบด้านล่างเหมือนเดิมทุกอย่าง):
- **02 · Media**: bus wrap ได้ลายพิมพ์สีจริงบนตัวรถ (แดง/เหลือง/เขียว/น้ำเงิน) แทนเส้นขาวล้วน,
  bus shelter เป็นหลังคาจั่ว+กล่องกระจก+จอโฆษณาเล็กด้านใน, billboard เป็นกรอบรูปภูเขา-พระอาทิตย์
  พร้อมจุดไฟด้านบน, จอ LED เป็นจอ+เส้นคลื่นสัญญาณ+จุดสัญญาณด้านบน
- **05 · Camera**: เปลี่ยนจากไดอะแกรม "รถ+กล้อง+กรวยมุมมอง" ที่คำนวณสดจากตำแหน่ง (`ic` ใน
  `ANGLES`) เป็น**ไอคอนคงที่ต่อมุม 1 รูป** (`ANGLE_ICONS` ใหม่ใน `src/js/20-chips.js`) เพราะภาพ
  ตัวอย่างใช้ภาพนิ่งเฉพาะจริงๆ ไม่ใช่ระบบคำนวณ: 3/4 หน้าฮีโร่ = รถสปอร์ต+ดาว, มุมต่ำ = กล้อง+เส้น
  เล็งขึ้นไปที่รถมุมหน้าตรง, โดรน = โดรน 4 ใบพัดเหนือรถเล็ก, ระดับสายตาคนเดิน = กล้อง+คนเดิน+รถ,
  โคลสอัพ = แว่นขยาย+ลูกศรซูม ฯลฯ — ฟิลด์ `ic` เดิมใน `10-data.js` เก็บไว้เผื่อใช้ในอนาคต ไม่มี
  ใครอ่านแล้วตอนนี้ ไม่กระทบ `p` ที่ส่งให้ AI
- **08 · Render Stack**: UE5 เป็นวงกลม+ตัว U, Octane เป็นเส้นซิกแซกเหลี่ยม, HDR เป็นกล้อง+รูรับแสง,
  Hyperreal เป็นเส้นโค้งซ้อนแบบลายนิ้วมือ, Auto Ad เป็นรถสปอร์ต, Clean Plate เป็นคนเดิน+ธง,
  Cinematic เป็นฟิล์มรีล, Ray Tracing เป็นปริซึม+กล่อง+รังสี, Light Trails เป็นเส้นคลื่นซ้อนหลายชั้น
- ตรวจแล้ว: เรนเดอร์ไอคอนทั้ง 24 ตัวแยกจาก build จริงในหน้าพรีวิวแยก (ไม่พังสักตัว, สีไล่เฉด/
  ป้ายชื่อ/ติ๊กถูกยังทำงานเหมือนเดิม) · `node build.mjs --check` ตรงทุกไบต์ทั้งสองเป้าหมาย ·
  `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — สแนปช็อตฐานแผนที่ถนน (MAP_SEED) แก้แผนที่โล่งเปล่าบน GitHub Pages
ผู้ใช้แนบภาพจับหน้าจอแผง "10 · Route Map" ที่ขึ้นแค่จุดหมุดลอยๆ ไม่มีถนนเลย (โลเคชั่น "มอเตอร์เวย์")
พร้อมถามหาวิธีดึงรูปแผนที่มาใช้งาน — ตรวจสอบแล้วพบสาเหตุจริง: เว็บที่ deploy จริง
(anusart-debug.github.io) ก็ยังดึงฐานแผนที่ถนนสดไม่ได้เหมือนกัน เพราะ **overpass-api.de
บล็อก origin `*.github.io` เอง** (คนละปัญหากับ origin null ของ `file://` ที่แก้ไปก่อนหน้านี้)
เดิมมีแค่ `ROUTE_SEED` (สายรถเมล์) เป็นสแนปช็อต แต่ไม่มีเส้นถนนให้วาดเลย ทุกจุดจึงตกไปที่
`drawPointOnly()` (จุดหมุด+วงรัศมีเปล่าๆ) เสมอบนโดเมนนี้ ไม่ว่าจุดไหน:
- **ดึงฐานถนน/น้ำ/สวน/ราง (`qBase`) ตรงจาก Overpass ด้วยตัวเอง** (ไม่ผ่านเบราว์เซอร์ จึงไม่ติด
  CORS ของ overpass-api.de เลย) แล้วประมวลผลด้วย Douglas-Peucker (epsilon ~3 ม.) ลดจำนวนจุด
  ต่อเส้นลงเหลือ 48% โดยไม่กระทบรูปทรงที่มองเห็นจริงที่สเกลจอ ~300-600px ของแอป เก็บ geometry
  เป็น `[lat,lon]` แทน `{lat,lon}` (ตัดคีย์ซ้ำนับหมื่นครั้ง ประหยัด ~30% เพิ่ม) ได้ไฟล์ใหม่
  `src/js/10c-map-seed.js` (~700 KB)
- **ครอบคลุม 15 จาก 22 โลเคชั่นในตอนนี้** — ระหว่างดึงข้อมูล overpass-api.de เริ่มจำกัดจำนวน
  คำขอใหญ่จากเครื่องเดียวในเวลาสั้นๆ (ยืนยันด้วยการทดสอบ TCP ตรงไปทั้ง 4 IP ของโดเมนนี้แล้วพบว่า
  บล็อกจริง ไม่ใช่แค่ 429 ที่ retry ผ่านได้) เหลือ 7 จุดที่ยังดึงไม่สำเร็จ: `planb`, `chatuchak`,
  `centralworld`, `ratchadamri`, `rama1`, `expressway`, `motorway` — จุดพวกนี้ (รวมถึง "มอเตอร์เวย์"
  ในภาพที่ผู้ใช้ส่งมาโดยตรง) จะยังเห็นแค่จุดหมุดจนกว่าจะดึงเพิ่มได้สำเร็จในรอบหน้า
- **ต่อ MAP_SEED เข้ากับโค้ดเดิมแบบไม่แตะของเดิม**: เพิ่ม `applyMapSeed(l)` ใน
  `src/js/52-overpass.js` แยกจาก `ROUTE_SEED` โดยตั้งใจ (บางจุดอย่าง "มอเตอร์เวย์" ไม่มีสายรถเมล์
  เลยแต่ก็ยังอยากเห็นถนนจริง) เรียกจาก `applySeedOrCache()` (ทันทีตอนเปลี่ยนโลเคชั่น) และจาก
  catch ของช่วง B ใน `loadMap()` (ตอนดึงสดล้มเหลว) — ทั้งสองจุดเดิมที่ `ROUTE_SEED` ใช้อยู่แล้ว
- **ไม่โกหกผู้ใช้ว่าเป็นข้อมูลสด**: เพิ่ม `MAPD.dataSource` ("live"/"seed") ตั้งค่าตามจริงทุกจุดที่
  `MAPD.data` ถูกเซ็ต แล้วต่อเข้าเครดิตแผนที่ในมุมขวาล่างของ `drawMap()` (`51-map-render.js`)
  ให้ขึ้น "ฐานถนนจากสแนปช็อต <วันที่>" เมื่อไม่ใช่ข้อมูลสด — คนละกลไกแต่แนวคิดเดียวกับป้าย
  "ข้อมูลตัวอย่าง" ของสายรถเมล์ที่มีอยู่แล้ว
- ตรวจแล้ว: `node build.mjs --check` ตรงทุกไบต์ทั้งสองเป้าหมาย (ไฟล์ใหญ่ขึ้นจาก ~420 KB เป็น
  ~1.1 MB ตามขนาดของ MAP_SEED) · `node test/run.mjs` ผ่านครบ 10 ไฟล์
- **งานต่อ**: ดึงฐานถนนของ 7 จุดที่เหลือเพิ่มตอนที่ผ่านช่วงจำกัดของ overpass-api.de ไปแล้ว
  (ปกติมักคลายภายในไม่กี่สิบนาทีถึงไม่กี่ชั่วโมงสำหรับการบล็อกแบบนี้)

## v41 (กำลังทำ) — ไทล์สีสำหรับการ์ดประเภทสื่อ (02) / มุมกล้อง (05) / สไตล์ (08)
ผู้ใช้ส่งภาพตัวอย่างไอคอนสื่อ Plan B 9 ช่อง (สี่เหลี่ยมมุมโค้งไล่สีสันสดใสต่างกันทุกช่อง ไอคอนสีขาว
ตรงกลาง ป้ายชื่อเป็นแถบทึบด้านล่าง) พร้อมขอ "ใช้ทำ icon สื่อต่างๆ ของ Plan B และใช้ทำ icon ใน
ส่วนข้ออื่นๆ ปรับใช้ให้เหมาะ สวยงาม ใช้งานได้จริง" — เปลี่ยนกล่องไอคอนสีเทาเรียบของการ์ด 3 หมวด
ที่ทำไปก่อนหน้า (ประเภทสื่อ/มุมกล้อง/สไตล์) ให้เป็นไทล์สีตามภาพตัวอย่าง โดยไม่ต้องวาดไอคอนใหม่เลย:
- เพิ่ม `TILE_PALETTE` (10 คู่สีไล่เฉด) + `tileGrad(idx)` ใน `src/js/20-chips.js` — ไล่สีตาม
  **ลำดับที่แสดง** ไม่ผูกกับ `id` เพื่อให้รายการที่ผู้ใช้เพิ่มเองต่อท้ายได้สีต่อคิวไปด้วยเช่นกัน
- รวม 3 ฟังก์ชัน `vehicleCardHTML`/`angleCardHTML`/`styleCardHTML` ให้เรียก `tileHTML()` ร่วมกัน
  ตัวเดียว — ไอคอนเดิมทั้งหมด (`VEHICLE_ICONS`/`STYLE_ICONS`/`angleIconSVG()`) **ไม่ต้องแก้แม้แต่
  บรรทัดเดียว** เพราะใช้ `currentColor` อยู่แล้ว แค่ตั้ง `color:#fff` ผ่าน CSS ก็เปลี่ยนเป็นสีขาว
  บนพื้นไทล์สีได้ทันที
- `renderChips()` ส่ง index ของแต่ละรายการเข้า `chipHTML()` เพิ่ม (เดิมไม่ส่ง) ให้ 3 ฟังก์ชันนี้ใช้
  เลือกสีจาก `tileGrad(idx)` — หมวดอื่น (โลเคชั่น/แสง) ไม่ใช้พารามิเตอร์นี้ ไม่กระทบ
- เพิ่มติ๊กถูกสีขาว (ใช้ `.chip.card .tick` ที่มีอยู่แล้ว) แทนกรอบสีแบรนด์เดิม เพราะพื้นเป็นสีสัน
  หลากหลายแล้ว กรอบสีเดียวไม่พอสื่อว่า "เลือกอยู่" ชัดพอ
- CSS ใหม่ (`.chip.card.icocard.tile`) ตั้ง specificity สูง (4 คลาส) กันไม่ให้ `.chip`/`.chip.card`
  ฐานทับสีไทล์โดยไม่ต้องพึ่งลำดับประกาศในไฟล์
- **ไม่แตะโลเคชั่น/แสง** — สองหมวดนั้นมีการ์ดข้อมูลประกอบของตัวเองอยู่แล้ว (บอกจำนวนสายรถเมล์/
  ช่วงเวลาจริง) ที่เน้นข้อมูลมากกว่าไอคอนหมวดหมู่ การใส่ไทล์สีแบบนี้จะลดความหนาแน่นข้อมูลลง
- ตรวจแล้ว: คลิกเลือกทั้ง 3 หมวดยังอัปเดต state/คำสั่งถูกต้อง ไม่มี pageerror ·
  `node build.mjs --check` ตรงทุกไบต์ทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — ปรับภาพร่างมุมกล้อง (05) ให้ใกล้เคียงภาพตัวอย่างที่ผู้ใช้ส่งมา
ผู้ใช้ส่งภาพตัวอย่าง 9 ช่อง (รถเมล์เส้นสมุดวาด + กล้อง DSLR จริง + เส้นกรวยมุมมองคู่) พร้อมขอ
"ใช้ตัวอย่างนี้แก้ไขภาพตัวอย่างในหัวข้อต่างๆ เพื่อความสวยงาม และดูเข้าใจง่ายขึ้น" — ยกระดับ
ไดอะแกรมมุมกล้องจากงานก่อนหน้า (จุดกลม+เส้นประเส้นเดียว) ให้ใกล้เคียงสไตล์ที่ส่งมา โดยยังคง
กลไกเดิมทั้งหมด (วาดสดจาก `ic:{cam,look,k}` ต่อรายการใน `ANGLES`, ไม่แตะ `p` ที่ส่งให้ AI เลย):
- **กล้อง**: เปลี่ยนจากจุดกลมเป็นรูปกล้องจริง (ตัวกล้อง+ช่องมองภาพ+วงแหวนเลนส์) วาดด้วย `<g>`
  ที่ตำแหน่ง `cam` เดิมทุกจุด
- **เส้นมุมมอง**: เปลี่ยนจากเส้นประเส้นเดียวเป็น "กรวยมุมมอง" 2 เส้นทึบลู่เข้าที่กล้อง — คำนวณ
  ด้วยการหมุนเวกเตอร์กล้อง→จุดเล็ง ±11° สดๆ ไม่ต้องเพิ่มพารามิเตอร์ต่อรายการเลยสักตัว
- **ตัวรถ**: จากกล่องสี่เหลี่ยมเปล่าเป็นรถเมล์ด้านข้างที่ดูออกว่าเป็นรถ (หลังคา+ช่องระบายอากาศ+
  หน้าต่าง 3 บาน+เส้นประตู+ล้อ) ตามภาพตัวอย่าง แต่ยังเรียบง่ายพอไม่แย่งพื้นที่จากกล้อง/กรวยมุมมอง
  ซึ่งเป็นตัวบอกความหมายจริงของมุมกล้องนั้นๆ
- ปรับตำแหน่ง `ic` ของ "ด้านข้างเต็มคัน" และ "มุมสูง โดรน" ในข้อมูลเล็กน้อยให้ไอคอนกล้อง/เส้นความสูง
  ไม่ล้นออกนอกกรอบ 64×44 หลังกล้องมีขนาดใหญ่ขึ้นจากทรงจุดกลมเดิม
- ขยายกล่องไอคอนของการ์ดมุมกล้องใน `styles.css` (`.icocard.anglecard .icotop`) จาก 52×36px
  เป็น 74×51px ให้เห็นรายละเอียดที่เพิ่มเข้ามาชัดขึ้น
- ตรวจแล้ว: เรนเดอร์ไดอะแกรมทั้ง 8 มุมแยกจากกันชัดเจน (ตรวจด้วยการแยกวาดทีละ SVG ที่ขนาดใหญ่)
  ไม่มี pageerror · `node build.mjs --check` ตรงทุกไบต์ทั้งสองเป้าหมาย · `node test/run.mjs`
  ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — ภาพร่างไอคอนช่วยเข้าใจ: ประเภทสื่อ (02) / มุมกล้อง (05) / สไตล์เรนเดอร์ (08)
ผู้ใช้ขอ "แก้ไขเพิ่มเติมในทุกข้อที่สามารถทำได้ เพิ่มภาพร่างตัวอย่างมุมกล้อง คล้ายๆ ในข้อ 7
ที่มีรูปตัวอย่างสัดส่วนภาพประกอบ เพื่อการเข้าใจการใช้งานที่ง่ายขึ้น" — สานต่อรูปแบบการ์ดที่มี
ภาพประกอบ (การ์ดโลเคชั่น/แสง/อัตราส่วนก่อนหน้า) ไปยัง 3 หมวดที่ยังเป็นชิปข้อความล้วน:
- **05 · Camera (มุมกล้อง)** — จุดสำคัญที่ผู้ใช้ระบุชื่อมาเอง: เพิ่มภาพร่างไดอะแกรมกล้อง
  ต่อรถ 1 ใบต่อมุม วาดสดจากตำแหน่งกล้องจริง ไม่ใช่ไอคอนคงที่ เพราะแต่ละมุมต่างกันที่ตำแหน่ง
  กล้องเทียบกับรถ ไม่ใช่แค่สัญลักษณ์เดียว — เพิ่มฟิลด์ `ic:{cam:[x,y],look:[x,y],k}` ต่อรายการ
  ใน `ANGLES` (`src/js/10-data.js`) แล้ววาดด้วย `angleIconSVG()` ใหม่ใน `src/js/20-chips.js`:
  เส้นพื้น+โครงรถเหมือนกันทุกใบ ต่างแค่จุดกล้อง/เส้นเล็งประ/ไอคอนเสริมตาม `k`
  (`low`=กล้องติดพื้น, `drone`=มีเส้นบอกความสูง, `eye`=เส้นระดับสายตา, `track`=เส้นแพนความเร็ว,
  `detail`=แว่นขยาย+กรอบซูมแทนจุดกล้อง) — ไม่แตะ `p` ที่ส่งให้ AI เลยแม้แต่ตัวอักษรเดียว
- **02 · Media (ประเภทสื่อ)** และ **08 · Render Stack (สไตล์)** — เพิ่มไอคอนเส้น (line icon)
  ต่อรายการแบบคงที่ (`VEHICLE_ICONS`/`STYLE_ICONS` ใน `20-chips.js` คีย์ตรงกับ `id` ใน
  `VEHICLES`/`STYLES`) ช่วยแยกรถเมล์ Wrap / 2 ชั้น / 3D / ป้ายรอรถ / ตุ๊กตุ๊ก / บิลบอร์ด / จอ LED
  และสไตล์เรนเดอร์ 9 แบบด้วยตาแทนการอ่านชื่อเฉยๆ
- ทั้ง 3 หมวดใช้ `.chip.card` เป็นฐานเดียวกับการ์ดก่อนหน้า (คลาส `.icocard` ใหม่ใน `styles.css`
  ทำแค่กล่องไอคอนด้านบน) — กลไกเลือก/ลบ/เพิ่มเองใน `mountChips()` ไม่ต้องแก้เลย รายการที่ผู้ใช้
  เพิ่มเอง (ไม่มีไอคอนที่นิยามไว้) ได้ไอคอนสำรอง (`*_ICON_DEFAULT`/ตำแหน่งกล้องเริ่มต้น) แทนพัง
- **ข้ามหมวด 03 · Brief และ 04/06/07 ที่ทำไปแล้ว** — Quick Tags เป็นคำที่พิมพ์แทรก ไม่มีภาพให้วาด
  ส่วนโลเคชั่น/แสง/อัตราส่วนมีการ์ดข้อมูลประกอบอยู่แล้วจากงานก่อนหน้านี้
- ตรวจแล้ว: คลิกเลือกทั้ง 3 หมวดยังอัปเดต state/คำสั่งถูกต้อง (ยืนยันด้วยตรวจ `aria-pressed`
  และเนื้อคำสั่งจริงหลังคลิก) · `node build.mjs --check` ตรงทุกไบต์ทั้งสองเป้าหมาย ·
  `node test/run.mjs` ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — การ์ด "สายรถเมล์" ลอยบนแผนที่ + แถบสรุปโลเคชั่น (ข้อ 3/10 · Route Map)
ผู้ใช้ส่ง mockup มา 1 ภาพ (แถบแท็บด้านบน, แผนที่มีหมุดสี, การ์ด "Bus Routes" ลอยมุมขวาบนเป็น
พิลสีพร้อมลูกศร, แถบล่างโชว์ "จุดที่เลือก" + สายรถเมล์ที่ผ่านเป็นพิลสี) พร้อมคำขอ "ปรับให้เข้ากัน
ทั้งใหม่และเก่า เพื่อความสวยงาม ใช้งานได้จริง" — แนวทางเดียวกับข้อ 4/6/7 ก่อนหน้า: **ไม่แตะข้อมูล/
กลไกจริงเลย เปลี่ยนแค่เปลือกการแสดงผล**:
- **ไม่ทำแท็บปลอม** — แท็บ 4 อันใน mockup (Location/Bus Route/Nearby Routes/Maps) ไม่ตรงกับ
  สถาปัตยกรรมแอปที่แยกเลือกโลเคชั่นไว้ที่แผง 04 อยู่แล้ว ใส่แท็บที่ไม่ทำงานจริงจะหลอกผู้ใช้ จึงข้าม
  ส่วนนี้ไป คงแผงแผนที่เป็นข้อมูลเดียวกับโลเคชั่นที่เลือกไว้เหมือนเดิม
- **การ์ด "สายรถเมล์" ลอยมุมขวาบนของแผนที่**: ย้าย `#routeLegend` (เดิมอยู่ใต้แผนที่) เข้าไปเป็น
  ลูกของ `#mapBox` ที่มี `position:relative` อยู่แล้ว วางลอยด้วย CSS (`#mapBox .routelegend`)
  โดยไม่แตะ JS render เดิมเลยแม้แต่บรรทัดเดียว — ใช้ CSS custom property `--rc` (ตั้งจาก
  `routeColour()` ที่มีอยู่แล้ว) ทำให้ทั้งปุ่มเป็นพิลสีล้วนแบบ mockup เฉพาะบริบทนี้ ส่วนโหมดเต็มจอ
  (`#fullLegend`) ยังเป็นชิปกรอบ+จุดสีแบบเดิมที่มีที่ว่างพอจะโชว์ต้นทาง-ปลายทางได้ — คนละ selector
  จึงไม่ชนกัน ระบบคลิกเพื่อ isolate สาย/ดูทั้งสาย/แสดงทุกสายทำงานเหมือนเดิมทุกประการ
  (verified/pair ที่ mockup ไม่มีที่ว่างโชว์ ย้ายไปเก็บใน `title=` ให้โฮเวอร์ดูได้แทนการทิ้งข้อมูล)
- ย้ายปุ่มซูม (`.maptools`) จากมุมขวาบนไปมุมขวาล่าง เปิดที่ให้การ์ดสายรถเมล์ ตรงกับตำแหน่งใน mockup พอดี
- **แถบ "จุดที่เลือก" ใต้แผนที่** (`#mapFoot`, ใหม่): `renderMapFoot()` ใน `src/js/51-map-render.js`
  อ่านข้อมูลชุดเดียวกับ `renderRouteLegend()` (`MAPD.groups`, `curLoc()`) ไม่ยิง Overpass เพิ่ม
  ไม่มี state ของตัวเอง เรียกจากท้าย `renderRouteLegend()` จึงอัปเดตพร้อมกันทุกจุดที่เคยเรียกฟังก์ชัน
  นั้นอยู่แล้ว (mapGo, loadMap สำเร็จ/ล้มเหลว, คลิกดูทั้งสาย ฯลฯ) — โชว์ชื่อโลเคชั่น + สายรถเมล์
  แบบพิลสีย่อสูงสุด 6 สาย พร้อม "+N" ถ้าเกิน
- `src/js/53-map-ui.js`: `closeFull()` เปลี่ยนจากคืน `#routeLegend` ไปเป็น sibling ใต้ `#stopBox`
  เป็นคืนกลับเข้า `#mapBox` ให้ตรงกับบ้านใหม่ (`openFull()`/ย้ายเข้า `#fullLegend` ไม่ต้องแก้)
- ตรวจแล้ว: คลิกไอโซเลตสาย, เปิด/ปิดโหมดเต็มจอ (ย้าย DOM ไป-กลับ), บันทึกใบนำเสนอ/PNG ยังทำงาน
  ปกติทุกอย่าง · `node build.mjs --check` ตรงทุกไบต์ทั้งสองเป้าหมาย · `node test/run.mjs` ผ่านครบ
  10 ไฟล์ (รวม `04-stages.test.js`/`06-resilience.test.js` ที่ตรวจ `#routeLegend .lgd-i` โดยตรง)

## v41 (กำลังทำ) — สื่อตัวอย่างจากคลัง Plan B + คำสั่งพร้อมใช้งาน 12 ปุ่ม (ข้อ 1)
ผู้ใช้ขอ 2 เรื่องในแผง "01 · Source": (1) ลิงก์ดาวน์โหลดภาพสื่อตัวอย่างสำหรับคนที่ไม่มีภาพของ
ตัวเอง (2) ปุ่ม "คำสั่งพร้อมใช้งาน" ตามภาพ mockup ที่มี 4 ปุ่ม ขอเพิ่มอีก 8 ปุ่มในสไตล์เดียวกัน
(ยืนยันสเปกกับผู้ใช้แล้ว: รวมเป็น 12 ปุ่ม, แต่ละปุ่มตั้งค่าทุกช่องที่เกี่ยวข้องอัตโนมัติ):
- **ลิงก์สื่อตัวอย่าง**: `<a>` ธรรมดาเปิดโฟลเดอร์ Google Drive ของ Plan B ในแท็บใหม่ ใต้กล่อง
  อัปโหลดภาพในแผง 01 — ไม่ใช่การเรียก API ใดๆ จึงไม่กระทบ CSP/allowlist ของไฟล์ออฟไลน์เลย
- **คำสั่งพร้อมใช้งาน**: แถบปุ่มเลื่อนแนวนอนใต้เฮดเดอร์ (ทุกโหมดเต็มจอ ไม่โผล่ทับโหมดแนะนำ
  เพราะเป็นทางลัดสำหรับคนที่คุ้นแอปแล้ว ไม่ใช่ของคนใหม่ที่ควรทำตามขั้นตอน) เพิ่ม `PRESETS`
  ใน `src/js/10-data.js` — 4 ปุ่มแรกคือของที่ผู้ใช้ส่งมา (คงข้อความปุ่มไว้คำต่อคำ) อีก 8 ปุ่ม
  แต่งขึ้นให้ครอบคลุมสื่อ Plan B ครบทั้ง 7 ประเภทและโลเคชั่นหลากหลาย แต่ละรายการอ้าง `id` จริง
  จาก `VEHICLES`/`LOCATIONS`/`ANGLES`/`LIGHTS` ที่มีอยู่แล้วเท่านั้น ไม่มีข้อมูลสมมติ
- `applyPreset()` ใน `src/js/20-chips.js` ตั้ง `S.vehicle/loc/angle/light/weather` พร้อมกัน,
  เติมข้อความตัวอย่างลงกล่องโจทย์เพิ่มเติมถ้าพรีเซ็ตมี, เรียก `renderChips()` ทุกหมวดที่เปลี่ยน,
  `renderBus()`+`mapGo()` ให้แผนที่/สายรถเมล์ตามโลเคชั่นใหม่ แล้ว `sync()` ปิดท้าย — ไม่แตะ
  `styles`/`ratio`/`lens` เพราะพรีเซ็ตนี้ตอบแค่ "ฉากคืออะไร" ผู้ใช้ยังปรับต่อได้ทุกช่องตามปกติ
- ตรวจแล้ว: `node build.mjs --check` ตรงทุกไบต์ทั้งสองเป้าหมาย, `node test/run.mjs` ผ่านครบ
  10 ไฟล์ (รวม `07-network.test.js` ที่คุมว่าไฟล์ออฟไลน์ไม่ยิงอะไรออกนอก allowlist — ลิงก์ Drive
  เป็นแค่ href ไม่ใช่ request จึงไม่กระทบ)

## v41 (กำลังทำ) — การ์ดเลือกอัตราส่วนภาพ (ข้อ 7)
ผู้ใช้ส่ง mockup มา 1 ภาพ (การ์ด 6 ใบ มีรูปทรงตัวอย่าง ป้ายอัตราส่วน คำอธิบายการใช้งาน และขนาด
พิกเซล) พร้อมคำสั่งเดิม "ผสมเก่าใหม่ให้ลงตัว ใช้งานง่าย สวยงาม" — ใช้แนวทางเดียวกับข้อ 4/6:
**เก็บตัวเลข `w`/`h` จริงของทั้ง 6 อัตราส่วนไว้ทั้งหมด แล้วเพิ่มแค่ฟิลด์แสดงผลใหม่**
- `src/js/10-data.js`: `RATIOS` เพิ่ม `use` (คำอธิบายการใช้งานสั้นๆ ภาษาไทย) ต่อรายการ —
  ไม่แตะ `w`/`h`/`ar`/`en`/`th`/`t`/`id` ที่ `ratioClause()`/`srcRatioClause()`/`dims()` ใช้อยู่
  คำสั่งจริงที่ส่งให้ AI จึงไม่เปลี่ยนเลย
- เปลี่ยน `<select id="ratio">` เป็น `<div class="chips ratiogrid" id="ratioChips">` ใน
  `src/index.html` — การ์ดแต่ละใบมีกรอบรูปทรงที่สเกลตามสัดส่วนจริง (`w:h` คำนวณสด) ป้าย
  อัตราส่วน คำอธิบายการใช้งาน และขนาดพิกเซล เพิ่ม `renderRatioCards()` + click handler ใน
  `src/js/20-chips.js` ให้ `S.ratio` อัปเดตแล้วเรียก `sync()` ทันที
- ย้ายจุดรับ event ออกจาก `31-inputs.js`/`63-wizard.js` (เดิมดัก `change` ของ `<select>`)
  เพราะการ์ดใช้ `click` แทน — `72-project.web.js` เปลี่ยนจากตั้ง `.value` เป็นเรียก
  `renderRatioCards()` ตอนโหลดโปรเจกต์จาก Drive
- `test/08-ratio.test.js`: แก้ 2 จุดที่เคย `selectOption('#ratio', N)` เป็น
  `click('#ratioChips [data-i="N"]')` — เทสต์เดิมที่ยืนยันว่าตัวเลขอัตราส่วนหลุดเข้าคำสั่งจริง
  (ไม่ใช่แค่ตัวเลือกโชว์เฉยๆ) ยังผ่านครบเหมือนเดิม
- ตรวจแล้ว: `node build.mjs --check` ตรงทุกไบต์ทั้งสองเป้าหมาย และ `node test/run.mjs`
  ผ่านครบ 10 ไฟล์

## v41 (กำลังทำ) — การ์ดแบบมีข้อมูลประกอบ: แสงและเวลา (ข้อ 6) + โลเคชั่น (ข้อ 4)
ผู้ใช้ส่ง mockup มา 2 ภาพ ขอให้ "ผสมเก่าใหม่ให้ลงตัว เน้นใช้งานง่าย ดูสวยงาม" — mockup มีทั้ง
ดีไซน์ใหม่และเนื้อหาที่ต่างจากของเดิม (แสง: ชื่อใหม่ + ช่วงเวลานาฬิกา + สลับ 2 แบบท้ายออก ·
โลเคชั่น: เป็นระดับเขต 12 เขต แทนถนน/แยกเจาะจง 21 จุด) จึงเลือกทางสายกลาง **เก็บข้อมูลจริง
ของเดิมไว้ทั้งหมด แล้วยกระดับการแสดงผลตาม mockup**:
- **ไม่ตัดตัวเลือกใดออก** — แสงยังครบ 8 แบบ (รวมสตูดิโอ HDRI กับย้อนแสงที่ mockup ตัดทิ้ง)
  โลเคชั่นยังครบ 21 จุดพร้อมพิกัด/สายรถเมล์ที่ตรวจสอบมาแล้ว การตัดออกคือการทิ้งข้อมูลจริง
  ที่หามาด้วยมือ ไม่ใช่การจัดดีไซน์
- **ไม่แตะข้อความ `p` ที่ส่งให้ AI** — เปลี่ยนแค่ชื่อที่แสดง (`th`/`en`) ให้สื่อความหมายขึ้น
  คุณภาพภาพที่ AI สร้างจึงไม่เปลี่ยนจากเดิมเลย
- `src/js/10-data.js`: `LIGHTS` เพิ่ม `time` (ช่วงเวลาที่แสงแบบนี้เกิดจริง) · `mood` (โทนสี
  ที่จะได้) · `g1`/`g2` (สีไล่เฉดบนหัวการ์ด) — ข้อมูลแสดงผลล้วน
- `src/js/20-chips.js`: `chipHTML()` รับ `key` เพิ่ม แล้วแตกเป็น `lightCardHTML()` /
  `locCardHTML()` เฉพาะ 2 หมวดนี้ หมวดอื่นยังเป็นชิปข้อความสั้นเหมือนเดิม — ทุกแบบคง
  `class="chip"` + `data-id` ชุดเดิม กลไกเลือก/ลบ/เพิ่มเองใน `mountChips()` จึงไม่ต้องแก้เลย
  การ์ดโลเคชั่นโชว์ "ผ่าน N สายรถเมล์" คำนวณสดจาก `busOf()` ทุกครั้ง ไม่ได้ฝังตัวเลขตายตัว
- **กับดัก TDZ ที่เจอจริงระหว่างทำ**: การ์ดโลเคชั่นเรียก `busOf()` ซึ่งอ่าน `BUSX` (เป็น `let`
  ใน `50-map-intro.js` ที่โหลด*หลัง* `20-chips.js`) — `let` ไม่ hoist ข้ามไฟล์ การ mount ชิป
  ตอนท้าย `20-chips.js` แบบเดิมจะพังทันทีตอนบูต ย้ายไปเป็น `mountAllChips()` ที่เรียกจาก
  boot block ท้าย `63-wizard.js` แทน (ตรงกับข้อเตือนเรื่องลำดับไฟล์ใน README)

## v41 (กำลังทำ) — Quick Tags สำหรับช่อง "โจทย์เพิ่มเติม"
เพิ่มปุ่มคีย์เวิร์ดยอดนิยม 12 คำใต้ช่อง `#detail` (ขั้นที่ 3 · Brief) ตามแบบที่ผู้ใช้ส่งมา
คลิกแทรกข้อความเข้ากล่องทันที คลิกซ้ำเพื่อลบ — ไม่มี state แยกเก็บว่าคำไหนถูกเลือกอยู่
เช็กจากข้อความในกล่องตรงๆ ทุกครั้ง (`indexOf`) ผ่าน `detailTagsSync()` ที่ผูกกับทั้ง event
คลิกปุ่มและ event `input` ของกล่องข้อความเอง ดังนั้นแก้ข้อความเองด้วยมือ (ลบคำออกตรงๆ)
ปุ่มจะไม่ค้างสถานะ "เลือกอยู่" ผิดๆ
- `src/js/10-data.js`: เพิ่ม `DETAIL_TAGS` (array ข้อความไทยล้วน)
- `src/js/31-inputs.js`: render ปุ่ม + toggle แทรก/ลบ + sync สถานะปุ่มกับเนื้อหากล่องข้อความ
- `src/index.html`: เพิ่ม `#detailTags` ใต้กล่อง `#detail`

## v41 (กำลังทำ) — เอาการวาดถนน/ระบายสีเส้นทางกลับมา + เผยแพร่ผ่าน GitHub Pages
ตอนตัดช่วง B (ฐานแผนที่ถนน) กับ C (ระบายสีเส้นทางรายสาย) ออกก่อนหน้านี้ เหตุผลคือ `file://`
เชื่อมต่อ `overpass-api.de` ไม่ได้เลย (CORS จาก origin null) ยิงช่วง B/C ต่อมีแต่เสียเวลาซ้ำ
ตอนนี้แอปเผยแพร่ผ่าน URL จริงได้แล้ว (GitHub Pages, repo เปลี่ยนเป็น public เพื่อใช้ Pages
ฟรีตามที่ผู้ใช้ยืนยัน) origin จึงไม่ใช่ null อีกต่อไป ข้อจำกัดเดิมหมดไป จึงเอาช่วง B/C กลับมา

- `src/js/52-overpass.js`: `loadMap()` เรียก `qBase()`/`qLines()` อีกครั้งหลังช่วง A สำเร็จ
  (โค้ดเดิมก่อนตัดออก ไม่ได้เขียนใหม่ — แค่ต่อกลับเข้าไป) `drawMap()` ใน `51-map-render.js`
  มีตรรกะวาดถนน/ระบายสีเส้นทางอยู่แล้วตลอด (ไม่เคยถูกลบ) แค่ไม่เคยได้ `MAPD.data` มาให้วาด
- `src/js/51-map-render.js`: handler ของ `#mapZoom`/`#mapNear` กลับไปเช็ก `MAPD.data` เพื่อ
  ตัดสินว่าต้อง `loadMap()` ใหม่ไหม (มีความหมายอีกครั้งเพราะ `MAPD.data` ถูกเติมค่าได้แล้ว)
- `docs/index.html` (ใหม่): สำเนาของ `dist/autostudio3d.html` สำหรับ GitHub Pages — ให้มี URL
  จริงบนเว็บ (`https://anusart-debug.github.io/Autostudio-3D/`) ไม่ใช่แค่ `file://` ในเครื่อง
  ต้องคัดลอกใหม่ทุกครั้งที่ build แล้วอยากอัปเดตหน้าเว็บ ไม่ใช่ไฟล์ที่แก้ตรงๆ
- ทดสอบแล้วด้วยการ mock คำตอบ Overpass ทั้ง 3 ช่วงจริง: ได้ถนน 22 เส้น ระบายสีเส้นทาง 6
  ช่วงถนน ไม่มี error — ยืนยันว่าโค้ดที่เอากลับมาทำงานถูกต้อง ไม่ใช่แค่ build ผ่าน

## v41 (กำลังทำ) — สแนปช็อตสายรถเมล์จริงในตัวแอป + เพิ่มโลเคชั่นพร้อมพิกัดได้เร็วขึ้น
ผู้ใช้ขอ 2 เรื่อง: (1) จุดที่มีตัวอย่างในแอพอยู่แล้วให้ขึ้นแสดงผลเร็ว (2) เพิ่มโลเคชั่นใหม่โดย
พิมพ์ได้รวดเร็ว — ทั้งสองแก้จากการค้นพบว่า `loadMap()` เดิมไม่มีการแคชผลลัพธ์ Overpass ถาวรเลย
(ดูหัวข้อก่อนหน้า: ยิงเน็ตใหม่ทุกครั้งไม่มีข้อยกเว้น และ `overpass-api.de` ปฏิเสธ CORS จาก
origin null ของ `file://` เสมอ — รวมกันแล้วคือรายชื่อสายว่างเปล่าตลอดสำหรับผู้ใช้ที่เปิดไฟล์ตรงๆ)

- `src/js/10b-route-seed.js` (ใหม่): สแนปช็อตข้อมูลสายรถเมล์จริงจาก `overpass-api.de` สำหรับ
  ทั้ง 21 โลเคชั่นตัวอย่างที่มากับแอป — **ดึงจริงครั้งเดียวเมื่อ 2026-09-20** ไม่ใช่ข้อมูลสมมติ
  (ยืนยันเช่น อนุสาวรีย์ชัยฯ พบ 53 สาย, เซ็นทรัลเวิลด์ 21 สาย) รูปร่างข้อมูลเดียวกับที่
  `loadMap()` ใช้ (stops+rels ดิบ ก่อนจัดกลุ่ม) ป้อนเข้า `groupRoutes()` ตัวเดียวกับ live fetch
  เสมอ สี/การเรียงลำดับ/การรวมทิศทางจึงตรงกันเป๊ะ ไม่ต้องเขียนตรรกะซ้ำ
- `src/js/52-overpass.js`: เพิ่ม `OVP_CACHE` (แคชถาวรใน localStorage คีย์ `as3d_ovpcache` —
  ทุกครั้งที่ `loadMap()` ดึงสำเร็จจริงจะบันทึกไว้ผ่าน `ovpCacheSave()`) และ `applySeedOrCache()`
  ซึ่งเลือกแคชถาวร (ถ้าเคยดึงสำเร็จมาก่อน) มาก่อนสแนปช็อตที่ฝังมากับแอปเสมอ
- `src/js/51-map-render.js`: `mapGo()` เรียก `applySeedOrCache()` ทันทีตอนเปลี่ยนโลเคชั่น (และ
  ตอนบูตแอปด้วย) ก่อนรอเน็ตเลย — เห็นรายชื่อสาย+สี+ต้นทาง-ปลายทางได้ทันทีสำหรับโลเคชั่นที่มี
  สแนปช็อต/แคช ใช้งานได้แม้ offline ทั้งหมด · `mapNote()` แยกป้ายบอกที่มาให้ตรงความจริง:
  "สดจาก OpenStreetMap" เฉพาะตอนเพิ่งดึงสำเร็จจริง (`OVP.source==="live"`) เท่านั้น ไม่งั้นบอก
  ตรงๆ ว่า "เคยดึงไว้เมื่อ [วันที่]" หรือ "ข้อมูลตัวอย่างในตัวแอป ([วันที่ดึงสแนปช็อต])" — ห้าม
  พูดว่าเป็นข้อมูลสดถ้าไม่ใช่ · `loadMap()` เมื่อดึงสดล้มเหลว (เช่นบน `file://`) จะเรียก
  `applySeedOrCache()` กู้คืนรายชื่อเก่ามาแสดงแทนปล่อยว่างเปล่า พร้อมบอกในข้อความว่าเป็นข้อมูล
  ที่มีอยู่ ไม่ใช่ข้อมูลสด · แก้ handler ของ `#mapZoom`/`#mapNear` ที่เช็ก `MAPD.data` (ตายแล้ว
  ตั้งแต่ตัดช่วง B/C ออก ทำให้เปลี่ยนความกว้างภาพ/รัศมีค้นหาไม่มีผลอะไรเลย) เป็น `resetView()`/
  `paintAll()` ตรงๆ
- `src/js/21-add-dialog.js`, `src/index.html`: เพิ่มช่อง **"พิกัด (ละติจูด, ลองจิจูด)"** ในกล่อง
  เพิ่มโลเคชั่นเอง (แสดงเฉพาะหมวดโลเคชั่น) วางจาก Google Maps ได้ตรงๆ (รูปแบบ `13.7650,
  100.5378` ตรงกับที่ Google Maps ให้ตอนคัดลอกพิกัด) ไม่ใส่ก็ได้เหมือนเดิม (ลากปรับบนแผนที่
  ทีหลัง) — ก่อนหน้านี้ต้องเพิ่มชื่อก่อนแล้วค่อยไปลากหมุดบนแผนที่ทีหลังเสมอ ตอนนี้พิมพ์จบในหน้าต่าง
  เดียวได้เลย **พบบั๊กจริงระหว่างทำ**: เลือกโลเคชั่นใหม่ที่พึ่งเพิ่ม (หรือแก้ไข/ลบ/จัดลำดับ
  โลเคชั่นเดิม) ไม่เคยเรียก `mapGo()` เลย ทำให้แผนที่ค้างโลเคชั่นก่อนหน้าทั้งที่ชิปที่เลือกอยู่
  เปลี่ยนไปแล้ว (บั๊กเก่าที่มีอยู่ก่อนงานนี้ ไม่ใช่ที่เพิ่งทำพัง) แก้โดยเรียก `mapGo()` ทุกจุดที่
  `addKey==="loc"` เปลี่ยนแปลงรายการ

## v41 (กำลังทำ) — ลดขอบเขตแผนที่: แสดงจุดโลเคชั่น + รายชื่อสายพร้อมสี ยังไม่วาดเส้นทาง
ต่อจากที่พบว่า `overpass-api.de` ปฏิเสธ CORS จาก origin null ของไฟล์ `file://` เสมอ (ดูหัวข้อ
ถัดไป) ผู้ใช้ขอให้ลดขอบเขตของฟีเจอร์แผนที่ลง: แสดงแค่จุดโลเคชั่นบนแผนที่ กับแจ้งรายชื่อสายรถเมล์
ที่ผ่านจุดนั้นพร้อมสีและต้นทาง-ปลายทางแยกสี ยังไม่ต้องวาดเส้นทางเดินรถจริงบนแผนที่ก่อน
- `src/js/52-overpass.js`: `loadMap()` ตัดช่วง B (ฐานแผนที่ถนน `qBase`) และช่วง C (ระบายสี
  เส้นทางรายสาย `qLines`) ออก เหลือแค่ช่วง A (ป้าย+สายที่จอด `qStops`) ซึ่งได้ทั้งรายชื่อสาย สี
  (`routeColour()`) และต้นทาง-ปลายทาง (`g.pair` จาก tag `from`/`to` ของ OSM relation) อยู่แล้ว
  — ฟังก์ชัน `qBase`/`qLines` ยังเก็บไว้ในโค้ดเผื่อกลับมาใช้ทีหลัง แค่ไม่ถูกเรียกแล้ว
- `src/js/51-map-render.js`: เพิ่ม `drawPointOnly()` — วาดจุดโลเคชั่น+วงรัศมี+ชื่อ โดยไม่ต้องมี
  ข้อมูลถนนเลย `drawMap()` เรียกฟังก์ชันนี้แทนการคืน `false` (placeholder ว่าง) เมื่อไม่มี
  `MAPD.data` แต่มี `MAPD.at` (พิกัดโลเคชั่น) `mapGo()` ตั้ง `MAPD.at`/`MAPD.title` จาก `locPos()`/
  `curLoc()` ที่รู้อยู่แล้วทันทีตอนเปลี่ยนโลเคชั่น (เรียกตอนบูตแอปด้วย) — แผนที่จึงเห็นจุดได้ทันที
  โดยไม่ต้องรอกดปุ่มหรือรอเน็ตเลย ใช้งานได้แม้ offline ทั้งหมด
- `src/js/53-map-ui.js`: เต็มจอ/บันทึกภาพแผนที่ (`openFull`/`fullPng`) เปลี่ยนเงื่อนไขจากเช็ก
  `MAPD.data` (ถนน ไม่มีอีกแล้ว) เป็นเช็ก `MAPD.at` (จุดโลเคชั่น) แทน ไม่งั้นสองฟีเจอร์นี้จะถูกปิด
  ถาวรเพราะ `MAPD.data` ไม่ถูกเติมค่าอีกต่อไป
- `src/js/55-sheet.js`: `painted(g)` เดิมเช็กว่าสายถูกระบายสีจริงบนแผนที่ (stage C) ก่อนให้จุดสี
  ในใบนำเสนอ — ตอนนี้ไม่มีการวาดเส้นทางบนแผนที่แล้ว จึงเปลี่ยนเป็นให้สีตามข้อมูลสดทุกสายเท่ากัน
  (`!!g.colL`) และเพิ่มข้อความต้นทาง-ปลายทาง (`g.pair`) ต่อท้ายชื่อสายในใบนำเสนอด้วย
- ปรับข้อความ UI ที่พูดถึง "วาดถนน/เส้นทางเดินรถ" ให้ตรงกับขอบเขตใหม่ (ปุ่ม, mapOff placeholder,
  toast ต่างๆ) — คงโค้ด "ดูทั้งสาย" (ดึง geometry เต็มสายแบบ on-demand ผ่าน `56-route-geom.js`/
  `57-route-view.js`) ไว้เฉยๆ เผื่ออนาคต ไม่ได้ลบ แต่ไม่ใช่ทางหลักอีกต่อไป

## v41 (กำลังทำ) — แก้ดึงแผนที่ช้ามาก/ไม่ได้เลย: ทดสอบจริงพบเซิร์ฟเวอร์สำรองทั้งคู่ใช้ไม่ได้
ผู้ใช้รายงานว่าดึงแผนที่และเส้นทางรถเมล์ใช้งานไม่ได้และช้ามาก (เจอ "พบ 0 สาย" ที่อนุสาวรีย์ชัยฯ
ทั้งที่จริงมีเป็นสิบสาย) — ตรวจด้วยการยิง `fetch` ตรงไปแต่ละเซิร์ฟเวอร์ในรายการสำรองทั้ง 3 ตัว
จริงๆ ทั้งจาก origin ปกติและจาก origin `null` (origin ที่ไฟล์ `file://` ใช้จริงเวลาฝ่ายขายเปิด
ไฟล์ตรงๆ ตามที่ README บอกให้ทำ) ไม่ใช่การเดา พบว่า **เซิร์ฟเวอร์สำรองทั้งสองตัวใช้งานไม่ได้จริง
ทั้งคู่ คนละแบบ**:
1. **`overpass.private.coffee` ค้างไม่ตอบเกิน 45 วินาที ไม่ว่า origin ไหน** — ไม่ใช่แค่ช้า
   แต่พังจริง ทุกครั้งที่เซิร์ฟเวอร์แรกล้มเหลวแล้วต้องหล่นมาคิวนี้ ผู้ใช้เสีย 20 วินาที
   (`OVP_TIMEOUT`) ไปฟรีๆ — นี่คือสาเหตุหลักของอาการ "ช้ามาก"
2. **`overpass.osm.ch` ตอบเร็วและยอม origin null ได้ปกติ (ต่างจาก overpass-api.de) แต่ฐานข้อมูล
   ว่างเปล่า/พัง** — ขอโหนดที่ยืนยันแล้วว่ามีอยู่จริงบน OSM (ป้ายรถเมล์ที่อนุสาวรีย์ชัยฯ) กลับได้
   `elements` ว่างทุกครั้ง พร้อม `timestamp_osm_base` ที่ไม่ใช่วันที่จริง (แค่เลข `"117081"`) —
   อันตรายกว่าอาการค้างเสียอีก เพราะมันตอบ "สำเร็จ" (HTTP 200) พร้อมผลลัพธ์เท็จว่าไม่มีสายรถเมล์
   ทั้งที่มีจริง นี่คือสาเหตุของอาการ "พบ 0 สาย" ที่ผู้ใช้เจอ — **ถ้าเผลอสลับให้ตัวนี้มาก่อนเฉยๆ
   จะดูเหมือนเร็วขึ้นแต่จริงๆ กลายเป็นให้คำตอบผิดเงียบๆ แย่กว่าเดิม จึงต้องถอดออกเช่นกัน**

เหลือ **`overpass-api.de` เพียงตัวเดียว** — เป็นอินสแตนซ์หลักที่ข้อมูลถูกต้องและไวที่สุด (คำสั่ง
เดียวกันเจอ 106 รายการสายรถเมล์จริงที่อนุสาวรีย์ชัยฯ) ข้อจำกัดเดียวคือปฏิเสธ CORS ทันทีเมื่อ
origin เป็น `null` — คือ **เปิดไฟล์ตรงจาก `file://` จะเชื่อมต่อเซิร์ฟเวอร์นี้ไม่ได้เลย** เป็น
ข้อจำกัดของเซิร์ฟเวอร์สาธารณะเอง ไม่ใช่บั๊กของแอป และไม่พบเซิร์ฟเวอร์ Overpass สาธารณะเจ้าไหน
(ทดสอบแล้ว 4 เจ้ารวม kumi.systems) ที่ยอม origin null พร้อมกับข้อมูลถูกต้องพร้อมกันเลย — บันทึก
ไว้ตรงๆ แทนที่จะเสี่ยงใส่เซิร์ฟเวอร์ที่ยังไม่ได้ตรวจเพิ่ม
- `src/js/51-map-render.js`: `OVP_HOSTS` เหลือเซิร์ฟเวอร์เดียว `overpass-api.de`
- `src/js/52-overpass.js`: `ovpFetch` แจ้งข้อความเฉพาะเจาะจงเมื่อเจอ CORS-null-origin แทน
  "เชื่อมต่อไม่ได้" เฉยๆ — บอกตรงๆ ว่าเป็นข้อจำกัดของเซิร์ฟเวอร์ ไม่ใช่บั๊ก แนะนำให้เปิดผ่าน
  เว็บแอพที่ deploy ไว้แทน (ซึ่ง origin จะไม่ใช่ null แล้ว ปัญหานี้หายไปเองทั้งหมด)
- `build.mjs`: `connect-src` เหลือแค่ `overpass-api.de`
- `test/06-resilience.test.js`, `test/07-network.test.js`: อัปเดตให้ตรงกับเซิร์ฟเวอร์เดียวที่เหลือ
- อัปเดต `README.md`, `docs/map-howto.md` ให้ตรงกับพฤติกรรมที่วัดได้จริง ไม่ใช่ที่เคยสันนิษฐานไว้

## v41 (กำลังทำ) — ตัดข้อกำหนดองค์กร/Internal ออกจากขั้นตอนล็อกอิน
ผู้ใช้ขอให้การตั้งค่าล็อกอินง่ายที่สุด: "ใช้แค่ email ที่ได้รับสิทธิ์ก็พอ ไม่ต้องตั้งค่าให้
ยุ่งยาก มีขอสิทธิ์ใช้งานแล้วแอดมินกดอนุมัติก็พอ" — โค้ดฝั่ง Firestore (`firestore.rules`,
`auCheckStatus`/`auRequestAccess`/หน้าอนุมัติ) ทำแบบนี้อยู่แล้วตั้งแต่เฟส 6 ไม่ผูกกับโดเมนเลย
แต่คอมเมนต์ในโค้ดกับ `docs/drive-setup.md` ยังพูดถึง "ต้องตั้ง OAuth consent screen เป็น
Internal" (ต้องมีองค์กร Google Workspace) ค้างมาจากแผนแรกก่อนเปลี่ยนมาใช้ Firebase Auth —
เป็นความเข้าใจผิดที่ทำให้การตั้งค่าดูซับซ้อนเกินจริง ทั้งที่ Internal ไม่จำเป็นเลยเมื่อความ
ปลอดภัยจริงมาจาก Firestore Security Rules อยู่แล้ว
- `src/js/70-auth.web.js`: เอา `provider.setCustomParameters({hd:"planbmedia.co.th"})` ออก
  จาก `auSignIn()` — ไม่ผูกกับโดเมนบริษัทแม้แต่ในระดับคำใบ้ UI · แก้คอมเมนต์ที่เข้าใจผิดว่า
  "Internal" คือกำแพงความปลอดภัยจริง ให้ตรงกับพฤติกรรมจริง (Firestore เท่านั้นที่เป็นกำแพง)
- `docs/drive-setup.md`: เขียนใหม่ทั้งไฟล์ — ตัดขั้นตอนที่บังคับให้ใช้โปรเจกต์ GCP เดิมที่มี
  องค์กร/Internal ออก เหลือแค่สร้างโปรเจกต์ Firebase ใหม่ด้วยบัญชี Google อะไรก็ได้ เปิด
  Firestore + Google sign-in (User type: External, Publishing status: In production) แจ้ง
  ตรงๆ ว่าผู้ใช้ใหม่จะเจอหน้าเตือน "Google hasn't verified this app" ครั้งแรก (กด Advanced →
  Go to app เพื่อผ่าน) เพราะไม่ได้ส่งตรวจสอบ — ยอมรับ trade-off นี้แทนความยุ่งยากขององค์กร
  หลังตั้งครั้งแรกจบ **ไม่ต้องกลับไปที่ Google Cloud/Firebase Console อีกเลย** อนุมัติผู้ใช้ใหม่
  ทุกคนทำในแอปนี้ที่เดียว (หน้า "อนุมัติผู้ใช้")

## v41 (กำลังทำ) — เฟส 7: Google Drive อ่าน/เขียนโปรเจกต์
- `src/js/71-drive.web.js` (ใหม่): wrapper REST v3 ของ Drive ด้วย scope `drive.file` เท่านั้น
  (เห็นแค่ไฟล์ที่แอปนี้สร้างเอง ไม่ใช่ทั้ง Drive) — สร้าง/หา root folder "AUTOSTUDIO 3D",
  สร้างไฟล์ด้วย `multipart/related` ที่ประกอบ body เอง (ไม่ใช้ `FormData`), อัปเดตด้วย
  `PATCH`+`uploadType=media`, ลบ = ตั้ง `trashed:true` เท่านั้น (กู้คืนได้ 30 วัน)
- `src/js/72-project.web.js` (ใหม่): แปลงสถานะแอป <-> `project.json` — `DV_FIELD_IDS` เป็น
  รายชื่อ field เดียวที่ทั้งบันทึกและโหลดใช้ร่วมกัน กัน save/load แยกกันเดินเมื่อเพิ่ม field ใหม่
  · `S.styles` (เป็น `Set`) แปลงเป็น `[...S.styles]` ตอนบันทึกและ `new Set(...)` ตอนโหลดเสมอ
  · แผนที่เก็บแค่ `at/radius/near/view/solo/bus/pin` ไม่ใช่ `MAPD` ทั้งก้อน (มีข้อมูล Overpass ดิบ
    หลายเมกะไบต์ + มี `Set` ซ้อนอยู่ข้างใน) — ดึงใหม่จาก Overpass ตอนเปิดโปรเจกต์แทน
- `src/js/73-drive-ui.web.js` (ใหม่): กล่องรายการโปรเจกต์ (เปิด/เปลี่ยนชื่อ/ทิ้ง) + บันทึก/
  บันทึกทับ พร้อมเช็ก `headRevisionId` ก่อนบันทึกทับกันคนอื่นแก้ไฟล์เดียวกันไปแล้ว
- `build.mjs`: เพิ่ม `https://www.googleapis.com` ใน `connect-src` ของเว็บ (โดเมนของ Drive API
  ที่ 71-drive.web.js เรียก) — ขอบเขตจริงถูกจำกัดด้วย scope `drive.file` ตอน sign-in ไม่ใช่ CSP
- **บั๊กที่เจอจากเทสต์ round-trip ก่อนส่งของ** (`test/10-drive-roundtrip.test.js`, ใหม่):
  ตอนแรกใส่ `llLat`/`llLng` ไว้ใน `DV_FIELD_IDS` ด้วย แต่พิกัดหมุดที่แท้จริงถูกเก็บผ่าน `PINX`
  (ดู `map.pin`) และ `mapGo()` ที่ `dvApplyProject` เรียกทีหลังจะเซ็ตช่องนี้ทับจากตำแหน่งของ
  โลเคชั่นเสมอ ทำให้ค่าที่บันทึกไว้ใน `fields.llLat/llLng` ไม่มีทางรอดถึงตอนโหลด — เอาออกจาก
  `DV_FIELD_IDS` เพราะมีกลไก `PINX`/`map.pin` ทำหน้าที่นี้อยู่แล้วจริงๆ
- เพิ่ม `window.__as3dProject` (เฉพาะเว็บ) ข้าง `window.__as3d` เดิม — เปิดช่องให้เทสต์เรียก
  `dvSerializeProject`/`dvApplyProject` ตรงๆ โดยไม่ต้องยิง Drive จริงเลย (ฟังก์ชันทั้งสองไม่เรียก
  `dvFetch` อยู่แล้ว รับ/คืนแค่ข้อมูลในหน่วยความจำ)

## v41 (กำลังทำ) — เฟส 6: ล็อกอิน Google + ระบบขอสิทธิ์การใช้งาน
เปลี่ยนจากแผนเดิม (Google Identity Services ตรงๆ อนุญาตทุกคนใน `@planbmedia.co.th`) เป็น
**Firebase Authentication + Firestore** เพราะต้องมีระบบขอสิทธิ์เป็นรายบุคคล (คนใหม่กดขอ
แล้วรอแอดมินอนุมัติ) ซึ่งต้องมีที่เก็บสถานะที่ตรวจสอบได้ปลอดภัย — Firestore Security Rules
ต้องมี `request.auth` จาก Firebase Auth เท่านั้น token ดิบจาก GIS ใช้ไม่ได้
- `src/js/70-auth.web.js` (ใหม่): sign-in ผ่าน `firebase.auth.GoogleAuthProvider`
  (ขอ scope `drive.file` พร้อมกันสำหรับเฟส 7) · เช็ก/ขอสิทธิ์การใช้งานผ่าน Firestore
  collection `users/<email>` · หน้าอนุมัติสำหรับแอดมิน (`ADMIN_EMAIL`) อนุมัติ/ปฏิเสธคำขอ
  ที่รอดำเนินการ · ล็อกส่วนแอปหลักไว้จนกว่าจะได้รับอนุมัติ (`dvRenderAll()`)
- `src/js/79-web-boot.web.js` (ใหม่): เรียก `auInit()` — ไม่ล็อกอินอัตโนมัติ ต้องกดเอง
- `firestore.rules`, `firestore.indexes.json` (ใหม่): ผู้ใช้อ่าน/สร้างคำขอของตัวเองได้
  (บังคับ `status=pending` กันตั้งอนุมัติเอง) แก้สถานะได้เฉพาะแอดมิน ห้ามลบ (เก็บร่องรอย)
- `build.mjs`: เว็บ (target=web) เปลี่ยน `script-src` จาก hash เป็น `'unsafe-inline'` —
  ตรวจแล้วว่า `firebase-auth-compat.js` สร้าง iframe ภายในที่รันอินไลน์สคริปต์ของมันเองตอน
  `firebase.auth()` ทำงาน (ไม่ใช่ตอนล็อกอิน) hash ของสคริปต์นั้นไม่แน่นอน/ไม่มีเอกสารทางการ
  ให้ pin ได้ และตาม CSP spec ถ้ามี hash-source ปนอยู่ `'unsafe-inline'` จะถูกเบราว์เซอร์
  เมินทันที จึงต้องไม่มี hash ปนเลยถ้าจะให้ `'unsafe-inline'` มีผลจริง — **ไฟล์ออฟไลน์ยังคง
  เป็น hash-only เข้มสุดเหมือนเดิม** เว็บชดเชยชั้นป้องกันนี้ด้วย Firestore Rules + OAuth
  consent screen Internal + scope `drive.file` แคบแทน · เพิ่ม `frame-src *.firebaseapp.com`
  (iframe auth ของ Firebase) + `connect-src` ให้ endpoint Firebase Auth/Firestore
- `build.mjs`: เพิ่มการเช็กว่า marker แต่ละตัว (`@@CSS@@`/`@@JS@@`/`@@CSP@@`) ต้องเจอ**พอดี
  1 ครั้ง** ไม่ใช่แค่ "เจอบ้าง" — เจอบั๊กจริงตอนพัฒนา: คอมเมนต์อธิบายพิมพ์ชื่อ marker ซ้ำไว้
  เป็นตัวอย่าง ทำให้ `.replace()` (แทนแค่ตัวแรกที่เจอ) ไปแทนที่คอมเมนต์แทนตัวจริง เหลือ
  `<script>@@JS@@</script>` ดิบๆ ในเว็บ พังทั้งหน้าแบบไม่มี error ตอน build เลย
- `docs/drive-setup.md` (ใหม่): ขั้นตอน Firebase Console ที่ต้องทำเอง (ภาษาไทย)

## v41 (กำลังทำ) — เฟส 5: build สองเป้าหมาย + CSP
- `build.mjs` สร้างได้สองไฟล์จากซอร์สเดียวกัน: `dist/autostudio3d.html` (ออฟไลน์ เหมือนเดิม
  ทุกประการ) และ `dist/web/index.html` (สำหรับ Firebase Hosting เฟส 6-7) — โมดูลเฉพาะเว็บใช้
  นามสกุล `.web.js` แล้วถูกกรองออกจริงตอน build ไฟล์ออฟไลน์ (ตัดออก ไม่ใช่ใส่ if)
- เพิ่ม Content-Security-Policy แบบ `<meta>` ให้ทั้งสองไฟล์ — `script-src` ใช้ sha256 hash
  ของสคริปต์จริง (คำนวณตอน build ไม่ใช่พิมพ์มือ) 2 ก้อน (สคริปต์กันจอกระพริบใน head +
  สคริปต์หลัก) ทำให้ XSS รันสคริปต์เพิ่มไม่ได้เลยแม้เจาะช่องอื่นได้
- `dist/web/` ขึ้น Firebase ได้พร้อม `firebase.json` ที่ build สร้างให้เอง (CSP header ตรงกับ
  meta เสมอเพราะมาจาก hash เดียวกัน)
- `test/07-network.test.js` เพิ่มยามคุมกลไกตัดบล็อกเว็บ/ออฟไลน์เอง + แก้ helper ในเทสต์ที่ใช้
  `fetch("data:...")` เป็น `atob()` ตรงๆ เพราะ CSP ใหม่บล็อก fetch ไปยัง data: URI (ตั้งใจ
  ไม่เปิดช่องนั้นเพราะแอปจริงไม่เคยต้องใช้)

- **ชุดทดสอบมีเขี้ยวจริงแล้ว**: เดิม `test/*.test.js` ทุกไฟล์แค่ `console.log` ผลออกมาแล้ว exit 0
  ไม่มี assertion เลย จึงไม่มีทางแดงได้จากการแก้คำสั่งหรือข้อมูลผิด — เพิ่ม `test/_expect.js`
  (`expect`/`expectEq`/`expectHas`/`done`) แล้วย้อนไปเติม assertion ในทั้ง 6 ไฟล์เดิม
- ติด marker คอมเมนต์ให้ query Overpass ทุกช่วง (`/*as3d:stops*/`, `/*as3d:base*/`, `/*as3d:lines*/`)
  ใน `src/js/52-overpass.js` แล้วแก้ dispatcher ของ `test/04-stages.test.js` และ `test/05-mapnav.test.js`
  ให้ match marker แทน `/foreach/`/`bus_stop` — กันไม่ให้ query ใหม่ในอนาคต (เช่น ดึง geometry รายสาย)
  ตกไปโดนโมฆะ mock ผิดตัวแบบเงียบๆ
- ลบ `qLinesFallback` (`52-overpass.js`) — ประกาศไว้ตั้งแต่ v39 แต่ไม่มีที่ไหนเรียกใช้เลย

## v40
- **แก้ต้นเหตุความช้า** ของการระบายสีเส้นทาง: คำถาม Overpass ช่วงที่ 3 เคยขอถนนทุกชั้น
  รวมทางเท้าและซอยบริการ ก่อนค่อยตัดกับสายรถเมล์ → กรองเหลือชั้นถนนที่รถเมล์วิ่งได้ และจำกัดรัศมี 900 ม.
- เพิ่ม **ซูม / เลื่อน / เต็มจอ / บันทึกภาพแผนที่** ทำงานกับข้อมูลที่โหลดมาแล้ว ไม่ยิงเน็ตใหม่
- มีนาฬิกาเดินบอกวินาทีระหว่างรอ และข้ามการระบายสีอัตโนมัติเมื่อมีเกิน 40 สาย
- เพิ่มช่องมองสถานะ `__as3d` สำหรับไล่บั๊ก

## v39
- **เปลี่ยนหลักการนับสาย**: จาก "สายที่วิ่งบนถนนในรัศมี" เป็น **"สายที่จอดป้ายรถเมล์ใกล้จุดสื่อ"**
  ตรงกับคำถามที่ฝ่ายขายต้องตอบจริง
- แยกการโหลดเป็น **3 ช่วง** (ป้าย+สาย → ฐานแผนที่ → เส้นทางรายสี) แสดงผลทันทีทีละช่วง
- แก้บั๊ก: `[]` เป็น truthy ทำให้วาดแผนที่เปล่าแล้วบอกว่าสำเร็จ
- แก้บั๊ก: ป้ายบอกรัศมีอ้างตัวแปรผิด
- ตรวจข้อมูลเซ็นทรัลเวิลด์ใหม่ทั้งหมด ตัดสายที่แหล่งข้อมูลขัดแย้งกันออก
- เพิ่มปุ่มตรวจใน Google Maps ที่ป้ายทุกป้าย

## v38
- สีแยกรายสาย วางเส้นขนานเมื่อใช้ถนนร่วมกัน คลิกเพื่อดูทีละสาย
- รวมขาไป-ขากลับของสายเดียวกัน (OSM เก็บเป็นคนละ relation)
- เพิ่มชื่อสถานี ย่าน สถานที่สำคัญ และชื่อถนน พร้อมระบบกันป้ายทับกัน

## v37
- **เลิกใช้ tile server และ Leaflet ทั้งหมด** หันมาวาดแผนที่เองบน canvas จากข้อมูล OSM ดิบ
  เพราะ `file://` ส่ง Referer ไม่ได้ ทำให้ทุกเจ้าปฏิเสธ (ไล่ตรวจมาแล้วทุกราย)
- กลับมาเป็นไฟล์เดียว ไม่ต้องใช้ตัวช่วยเปิดเซิร์ฟเวอร์

## v36
- ลองทางแก้ 403 ด้วย iframe ฝังของ OSM + ตัวเปิดเซิร์ฟเวอร์ในเครื่อง (ภายหลังเลิกใช้ใน v37)

## v35
- เพิ่มแผนที่และสายรถเมล์ต่อโลเคชั่น พร้อมใบนำเสนอฝ่ายขาย

## v32
- **ถอดระบบ API ออกทั้งหมด** ตามคำสั่งผู้ใช้ เพื่อความปลอดภัย
  ไม่มีช่องใส่ key ไม่เจนภาพเอง เหลือเฉพาะการเขียนคำสั่งและคัดลอก
- เหตุผล: ไม่มีทางฟรีทางไหนทำ image-to-image ได้จริงโดยไม่มีบัญชีหรือบิลลิ่ง (ทดสอบครบทุกเจ้า)
