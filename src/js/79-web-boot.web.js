/* ---------------- จุดเริ่มของโค้ดเว็บ (เฉพาะเว็บ) ----------------
   อยู่ท้ายสุดของ _order.json โดยตั้งใจ — ทุกอย่างของแอปโหลดและ sync() ไปแล้ว
   (63-wizard.js เปิดโหมดแนะนำอัตโนมัติไปแล้วถ้าตั้งไว้) ไฟล์นี้แค่เริ่ม Firebase Auth
   ห้ามเรียกล็อกอินอัตโนมัติตรงนี้ — เป็นการกดของผู้ใช้เองเท่านั้น (ทั้งเพื่อไม่ให้ popup OAuth
   ไปซ้อนทับโหมดแนะนำที่เพิ่งเปิด และเพื่อไม่ให้ขอ token แบบเงียบๆ ทุกครั้งที่เปิดหน้า) */
auInit();

